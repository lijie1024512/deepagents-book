---
name: novel-outline-generator
description: 用于生成高质量小说大纲的Skill，适用于各类同人小说和原创长篇小说创作。以"创意引擎"为核心驱动，能够根据用户提供的基础设定，生成脑洞独特、冲突不可逃避、逻辑严密的详细章节大纲。支持引擎驱动的弧线规划、滚动生成、中断恢复和案例对标（V3.0创意引擎版）。
dependency:
  python:
    - pyyaml>=6.0
---

# 小说大纲生成器

## 触发场景示例

### ✅ 会触发本Skill的场景（执行完整流程）

1. **生成新大纲**
   - 用户："帮我生成一个海贼王同人小说大纲"
   - 用户："设计一个海军主角的故事线"

2. **续写大纲**
   - 用户："请根据现在的大纲续写5章"
   - 用户："根据大纲规划第21-30章的内容"
   - 用户："设计后续5章的大纲"

3. **优化大纲**
   - 用户："优化第5-7章的大纲"
   - 用户："调整大纲，让情节更具体"
   - 用户："修改大纲，收服索隆"

4. **改变剧情**
   - 用户："让主角收服索隆"
   - 用户："改变第10章的剧情"
   - 用户："影响路飞的发展"

5. **蝴蝶效应推理**
   - 用户："分析收服索隆的影响"
   - 用户："推演蝴蝶效应"
   - 用户："如果主角收服索隆，路飞会怎样"

### ❌ 不会触发本Skill的场景（写正文，不是写大纲）

1. **续写小说正文**
   - 用户："续写第4章的内容"（这是写正文）
   - 用户："写第X章内容"（这是写正文）

**关键区别**：
- ✅ **大纲**：规划章节结构、情节点、冲突、爽点、伏笔
- ❌ **正文**：写出具体的对话、描写、场景

---

## 任务目标
- 本Skill用于：生成、续写、优化高质量的小说大纲
- 能力包含：情节设计、角色塑造、逻辑验证、爽点布局、因果链推理、蝴蝶效应分析
- 触发条件（满足以下任一条件即触发）：
  - **生成新大纲**：用户提供小说基础信息（世界观、主角设定、故事线框架等）
  - **续写大纲**：用户要求"续写大纲"、"根据大纲规划后续章节"、"设计第X-Y章大纲"等
  - **优化大纲**：用户要求"优化大纲"、"修改大纲"、"调整大纲"等
  - **改变剧情**：用户要求"收服XXX"、"改变XXX事件"、"影响XXX角色"等
  - **蝴蝶效应推理**：用户要求"分析改变的影响"、"推演蝴蝶效应"、"如果...会怎样"等

**重要说明**：
- ❌ **不触发本Skill**：用户要求"续写小说正文"、"写第X章内容"、"生成正文"等（这是写正文，不是写大纲）
- ✅ **触发本Skill**：用户要求涉及"大纲"、"规划"、"设计"、"改变剧情"、"蝴蝶效应"等

## ⚠️ 重要：强制交互式要求

**在使用本技能时，必须严格遵守以下规则，禁止跳过任何步骤：**

### ⚠️ 文件操作要求（续写大纲时）
- ✅ **必须**：使用 `read_file` 工具读取文件
- ❌ **禁止**：使用 shell 命令检查文件（如 `shell("type ...")`、`shell("wc -l ...")`、`shell("powershell ...")` 等）
- ❌ **禁止**：使用任何 shell 命令来检查文件行数、内容等
- ✅ **必须**：直接使用 `read_file` 工具读取文件内容

### 核心要求：所有场景都必须执行因果链推理

**无论是生成新大纲、续写大纲还是优化大纲，都必须执行因果链推理，分析不同的剧情走向，生成3个分支供用户选择。**

- ✅ **生成新大纲**：必须执行因果链推理，分析故事框架下的不同发展路径
- ✅ **续写大纲**：必须执行因果链推理，分析续写部分的不同剧情走向
- ✅ **优化大纲**：必须执行因果链推理，分析优化的不同方向和策略

**禁止跳过因果链推理，直接生成、续写或优化大纲。**

### 续写大纲的特殊说明
当用户要求续写大纲时，遵循标准流程，但需要注意：

**⚠️ 重要：续写大纲的执行顺序**
1. 第1步：世界观识别（如果已加载可跳过）
2. 第2步：需求收集（从用户输入识别文件路径，读取现有大纲）
3. **第3步：因果链推理（必须执行，生成3个分支供用户选择）**
4. **等待用户选择分支**
5. 第4步：大纲生成（用户选择分支后才能执行）

**续写时的特殊要求**：
- **必须从用户输入中识别文件路径**，使用用户指定的原始小说文件（如 `e:\git\deepagents-book\novels\海贼王之唯我独尊.md`）
- **必须使用 `read_file` 工具读取文件**，禁止使用 shell 命令检查文件
- **禁止使用任何 shell 命令**：包括但不限于 `shell("type ...")`、`shell("wc -l ...")`、`shell("powershell ...")`、`shell("Get-Content ...")` 等
- **如果需要检查文件内容或行数**：直接使用 `read_file` 工具读取文件，不要使用 shell 命令
- **禁止**读取或编辑内存文件（如 `/memory/user/AGENTS.md`）- 这是 agent 的内存文件，用于存储用户偏好，续写大纲时不应编辑
- **禁止**在生成分支之前创建 todo 列表（write_todos）
- **禁止**在生成分支之前开始编写大纲
- **禁止**在生成分支之前规划剧情走向
- **禁止**在用户选择分支之前，执行任何生成或编辑操作
- 续写时，第2步（需求收集）可以简化，直接根据用户原始需求确定续写范围
- 续写时，第4步（大纲生成）后需要将内容合并到用户指定的原始小说文件

## 完整链路设计

### 链路总览

```
前置：案例准备（本地文件 或 网络搜索）
  ↓
第0步：脑洞 + 引擎设计（一体化）        → 输出 engine.md
  ↓
第1步：世界观识别与加载
  ↓
第2步：需求收集 + 主角设定（从引擎推导）
  ↓
第3步：引擎驱动的弧线规划（5-10章一批）  → 输出 outline.md
  ↓
第4步：渐进式大纲生成（每批5-10章）
  ↓
每批后：引擎健康检查 + 用户反馈收集       → 更新 progress.md
```

### 项目文件结构（持久化，支持中断恢复）

```
novels/项目名/
├── engine.md           ← 脑洞+引擎+弧线骨架（灵魂文件）
├── outline.md          ← 当前大纲（滚动更新）
├── chapters/
│   ├── ch01.md         ← 各章正文
│   ├── ch02.md
│   └── ...
├── style_notes.md      ← 风格参考（案例风格索引/网搜结果）
└── progress.md         ← 进度+用户反馈+下批方向
```

### 案例来源优先级

```
优先级1：用户提供本地文件
  → 分阶段读取（前3章/4-15章/关键转折），提取风格和引擎模式
  → 保存到 style_notes.md，后续不用重复读

优先级2：用户知道书名但没有文件
  → 网络搜索免费章节（起点/番茄前几章通常免费）
  → 搜索读者评论/书评（提取"读者觉得爽的点"）
  → 保存到 style_notes.md

优先级3：用户不知道具体书名
  → 搜索该世界观下的热门同人排行榜
  → 找高分作品，搜其内容/评论
  → 保存到 style_notes.md

优先级4：什么都搜不到
  → 回退到方法论文档（creative_hooks.md等）
  → 质量会降低，但有框架兜底
```

## 前置准备
- **无需外部文件**：所有信息通过对话从用户处获取
- **参考文档**：本Skill包含完整的参考文档，Agent应直接读取使用
- **案例参考**：支持读取本地案例文件或网络搜索案例，写作时对标参考
- **输出格式**：Markdown格式的大纲文件，保存到项目文件结构中

## 续写和优化的记忆机制

### 智能体记忆范围

在续写和优化时，智能体会记住以下信息：

#### 1. **已生成的设定**
- 主角设定：姓名、性格、金手指、背景、初始状态
- 故事框架：主角目标、核心冲突、起点
- 世界观：已加载的世界观知识库（海贼王/火影/死神）
- 蝴蝶效应：已执行的蝴蝶效应推理和用户选择

#### 2. **已生成的剧情**
- 角色状态：当前能力、性格、关系、位置
- 势力状态：当前势力分布、关系
- 伏笔：已埋下的伏笔
- 时间线：当前进度（第几章、第几卷）

#### 3. **用户的偏好**
- 改变偏好：用户喜欢改变哪些原作内容
- 交互偏好：用户喜欢详细的逐步推理还是简洁的分支选择
- 修改偏好：用户经常修改哪些方面（情节、角色、爽点）

### 记忆的作用

1. **减少重复询问**
   - 智能体会记住主角的设定，不会反复询问相同的问题
   - 例如：第一次生成时问"主角的姓名"，续写时不会重复询问

2. **确保连贯性**
   - 智能体会记住已生成的剧情，确保续写内容连贯
   - 例如：主角在第10章获得"武装色霸气"，第21章不会忘记这个设定

3. **支持蝴蝶效应推理**
   - 智能体会记住已执行的蝴蝶效应，推理后续影响
   - 例如：第1章收服索隆，续写第21章时会分析这个改变对后续剧情的影响

4. **保留用户选择**
   - 智能体会记住用户选择的分支，确保续写内容符合用户期望
   - 例如：用户选择"平衡策略"，续写时会延续这个策略

### 记忆的局限性

**智能体不会记住**：
1. **用户的临时想法**：用户提到的但没有确定的想法
2. **未选择的内容**：用户没有选择的分支内容
3. **已放弃的设定**：用户明确放弃的设定

**用户可以主动覆盖记忆**：
- 如果用户希望改变某个设定，智能体会更新记忆
- 例如：用户可以要求"主角的金手指改为加点系统"，智能体会更新记忆

### 续写和优化时的行为

#### 续写时：
1. 智能体会读取现有大纲，提取当前状态
2. 基于记忆中的设定和剧情，生成续写内容
3. 如果用户希望改变某个设定，智能体会更新记忆并执行因果链推理

#### 优化时：
1. 智能体会读取现有大纲，提取当前状态
2. 基于记忆中的设定和剧情，应用8项优化原则
3. 如果优化涉及剧情改变，智能体会执行因果链推理

---

## 操作步骤（续写和优化的记忆）

### 标准流程（智能体主导，强制对话式交互）

**⚠️ 重要执行顺序：**
1. **第0步：脑洞 + 引擎设计（新项目必须，续写跳过）**
2. 第1步：世界观识别与加载
3. 第2步：需求收集（主角设定从引擎推导）
4. **第3步：引擎驱动的弧线规划（替代原因果链推理）**
5. **等待用户选择方案后**，才能执行第4步
6. 第4步：滚动式大纲生成（5-10章一批）

**关键禁止项（续写大纲时）**：
- ❌ **禁止**：跳过第3步（弧线规划），直接执行第4步（大纲生成）
- ❌ **禁止**：在用户选择方案之前，创建 todo 列表（write_todos）
- ❌ **禁止**：在用户选择方案之前，开始编写大纲
- ❌ **禁止**：在用户选择方案之前，编辑任何文件
- ❌ **禁止**：读取或编辑 `/memory/user/AGENTS.md`（这是 agent 的内存文件，用于存储用户偏好，不是用户指定的文件，续写大纲时不应编辑）
- ❌ **禁止**：直接规划剧情走向，必须先执行引擎驱动的弧线规划

#### 第0步：脑洞 + 引擎设计（新项目必须执行）

**脑洞和引擎必须一起设计，不可分割。** 脑洞是种子，引擎是种子的生长机制。

**⚠️ 重要**：这一步是整个创作的灵魂，决定了小说的创意独特性和长线吸引力。

**必须读取**：`references/creative_hooks.md` — 了解12种脑洞类型和引擎设计方法论
**必须读取**：`references/successful_premises.md` — 了解成功案例作为对标
**必须读取**：`references/title_engineering.md` — 标题设计指南

**案例收集（在引擎设计之前或同时进行）**：
1. 询问用户：是否有推荐的好书/类似的作品？是否有本地文件？
2. 如果有本地文件：读取前3章，提取风格和引擎模式，保存到 `style_notes.md`
3. 如果有书名但无文件：网络搜索该书的免费章节和读者评论
4. 如果没有具体书名：搜索该世界观下的热门同人排行榜
5. 将收集到的风格参考和引擎分析保存到 `style_notes.md`

**执行流程**：

1. **生成至少3个脑洞+引擎一体化方案**

   每个方案必须包含以下完整结构：
   ```
   方案X：「标题（冒号格式：世界观：核心困境）」

   脑洞：一句话描述核心设定

   引擎：
     核心矛盾：XX与YY不可调和
     驱动力：什么力量在持续推动冲突？
     升级机制：小→中→大→超大
     不可停止：为什么主角无法喊停？
     代价：用优势解决问题时要付出什么？

   弧线预览（3-5个弧）：
     弧1：引擎启动——初始冲突
     弧2：引擎加速——冲突扩大
     弧3：引擎过载——更高层级矛盾
   ```

2. **不可逃避性四问检验（每个方案必须全部通过）**
   - 能选择"不管"吗？→ 必须不能
   - 冲突会自动找上门吗？→ 必须会
   - 困境会升级吗？→ 必须会
   - 优势有代价吗？→ 必须有

3. **引擎强度对标检验**
   - 自驱力：和"汉末魅魔"的被动吸引力相比，是否同样自动？
   - 升级性：和"转生眼"的暴露风险升级相比，是否同样递进？
   - 不可停止性：和"武大郎"的倒计时相比，是否同样无解？

4. **标题设计**
   - 必须使用冒号标题格式：「世界观：核心困境」
   - 标题必须通过标题自检四问
   - 为每个方案生成1-2个标题变体

5. **展示方案供用户选择**
   - 展示每个方案的完整信息（脑洞、引擎、弧线预览、标题）
   - 展示四问检验结果和引擎强度对标
   - 等待用户选择或提出修改意见
   - 用户可以组合不同方案的元素

6. **确定方案后，写入 `engine.md`**
   - 将选定的脑洞+引擎+弧线骨架写入项目的 `engine.md`
   - 这是整个项目的灵魂文件，后续所有步骤都要参考

**续写时跳过第0步**：如果是续写大纲，直接读取已有的 `engine.md`，不需要重新设计引擎。

#### 第1步：世界观识别与加载（必须）
通过对话识别用户要创作的同人类型，**必须先加载对应的世界观知识库**（只读取一次，后续记住）：

- 海贼王同人：必须读取 `references/onepiece_knowledge_base.md`
- 火影同人：必须读取 `references/naruto-world.md`
- 死神同人：必须读取 `references/bleach-world.md`
- 其他同人：根据具体世界观加载相应文档

**加载目的**：确保 AI 不会胡编乱造，严格遵循原作设定（角色性格、能力体系、剧情逻辑等）

#### 第2步：需求收集（强制对话式，从引擎推导）
**必须通过对话逐步从用户处获取信息，禁止一次性推断所有信息**：

**⚠️ 核心变化**：主角设定不再凭空收集，而是**从引擎推导**。引擎决定了主角必须面对什么困境，困境决定了主角需要什么性格和能力。

- **生成新大纲时**：
  - 主角设定（从引擎推导）：
    - 姓名、背景、初始状态
    - 性格：**必须与引擎冲突产生化学反应**（如引擎是"被追杀"，性格可以是"冷静但有底线"，底线会被引擎不断挑战）
    - 金手指/能力：**必须是引擎的一部分**（如果金手指与引擎无关，那引擎设计有问题）
    - 致命弱点：**来自引擎的代价性**（使用能力的副作用/引擎的反噬）
  - 故事框架（从引擎推导）：
    - 主角目标：**与引擎冲突相关**（不是随机的"变强"）
    - 核心冲突：**就是引擎本身**
    - 起点：引擎启动的位置
  - 改变需求（如果有）：是否收服原作角色、是否改变原作事件等
  - 章节规划：**不一次性规划全书**，先规划前5-10章
- **续写大纲时**：
  - 从用户输入中识别文件路径和续写范围（如"续写接下来的5章"）
  - **必须使用 `read_file` 工具读取现有大纲文件**，提取当前状态
  - **禁止使用 shell 命令检查文件**（如 `shell("type ...")`、`shell("wc -l ...")`、`shell("powershell ...")`、`shell("Get-Content ...")` 等）
  - 根据用户原始需求确定续写范围，无需重复询问

**执行方式**：
1. 每次只问1-2个问题
2. 等待用户回答后，再问下一个问题
3. 禁止一次性推断所有信息并生成大纲
4. **续写时**：禁止在生成分支之前创建 todo 列表或开始编写大纲
5. **续写时**：禁止使用 shell 命令，必须使用 `read_file` 工具

#### 第3步：引擎驱动的弧线规划（**强制交互式，所有场景都必须执行**）
**⚠️ 重要：无论是生成新大纲、续写大纲还是优化大纲，都必须执行引擎驱动的弧线规划。所有冲突必须源于引擎，不是随机事件。**

**⚠️ 关键：这是大纲生成的核心步骤，禁止跳过！**

**核心原则**：
- **不要一次性规划全书**。只详细规划接下来的5-10章，保持长线方向感
- **每章冲突必须来自引擎**，不是随机事件
- **弧线结尾必须自然导向下一弧**（解决A产生B）
- **删掉任何一章，引擎链条应该会断**（每章都有存在意义）

**执行顺序（必须严格遵守）**：
1. ✅ **必须**：读取 `engine.md`（续写时）或使用第0步确定的引擎
2. ✅ **必须**：基于引擎规划弧线，确保冲突源于引擎
3. ✅ **必须**：生成3个弧线方案供用户选择，等待用户选择
4. ✅ **必须**：用户选择方案后，才能创建 todo 列表或开始生成大纲

**关键禁止项**：
- ❌ **禁止**：跳过弧线规划，直接创建 todo 列表（write_todos）
- ❌ **禁止**：跳过弧线规划，直接开始编写大纲
- ❌ **禁止**：规划与引擎无关的随机冲突
- ❌ **禁止**：在生成方案之前，创建 todo 列表
- ❌ **禁止**：在用户选择方案之前，开始生成大纲
- ❌ **禁止**：在用户选择方案之前，编辑任何文件（包括 `/memory/user/AGENTS.md`）

**必须执行以下流程，禁止跳过**：

1. **确认引擎状态**
   - **生成新大纲时**：使用第0步确定的引擎，确认引擎的当前阶段
   - **续写大纲时**：读取 `engine.md` + 最近2-3章正文 + `progress.md`，恢复引擎状态
   - **优化大纲时**：读取现有大纲，确认引擎在各章的运转状态
   - 确认引擎核心矛盾、当前驱动力、升级阶段

2. **弧线规划（基于引擎，只规划最近5-10章）**

   弧线规划格式：
   ```
   弧线X：篇名（第N-M章）
     引擎状态：[低速/中速/高速/过载] —— 描述引擎在这一弧的运转状态
     核心冲突：[来自引擎的冲突描述]

     章节骨架：
     第N章：[引擎事件] → [主角应对] → [新的引擎压力]
     第N+1章：...
     ...

     弧线终点→下一弧起点：
     [解决A产生B的描述]
   ```

   - **如果有明确的改变需求**（如"收服索隆"）：
     - 改变必须通过引擎来推动，不是凭空发生
     - 分析改变对引擎运转的影响
     - 保留因果链推理：分析直接影响→连锁反应→世界修正
   - **用户可以补充信息**：用户可以主动提供额外的设定或约束

3. **逐步推理（智能体主导，每步都等待用户反馈）**
   - **第1步：分析引擎在这一弧的状态**
     - 引擎是在加速还是在空转？需要升级吗？
     - 等待用户确认或补充
   - **第2步：规划引擎驱动的冲突序列**
     - 每章的核心冲突必须回溯到引擎
     - 等待用户确认或补充
   - **第3步：设计弧线的升级节点**
     - 弧线中间需要有小高潮，弧线结尾需要引擎升级
     - 等待用户确认或补充
   - **第4步：检查弧线衔接**
     - 这一弧的结尾如何自然导向下一弧？
     - 等待用户确认或补充
   - **用户随时可以补充**

4. **生成3个弧线方案（必须生成，基于用户反馈调整）**
   - 智能体基于引擎状态和用户反馈，生成3个弧线方案
   - **生成新大纲时**：
     - 方案A：保守型弧线（引擎低速运转，积蓄力量）
     - 方案B：平衡型弧线（引擎中速，冲突与成长并行）
     - 方案C：激进型弧线（引擎高速，密集冲突快速升级）
   - **续写大纲时**：
     - 方案A：延续当前节奏（引擎状态不变）
     - 方案B：引擎升级（引入新维度/新势力）
     - 方案C：引擎转向（从一种冲突切换到更大的冲突）
   - **优化大纲时**：
     - 方案A：保守优化（强化引擎连接，最小改动）
     - 方案B：平衡优化（调整引擎节奏，适度改进）
     - 方案C：激进优化（引擎重构，大幅改进）
   - **用户可以修改方案**：用户可以对方案提出修改意见

5. **弧线质量检查（每个方案都必须通过）**
   - 每章冲突是否来自引擎？（不是随机事件？）
   - 弧线结尾是否自然导向下一弧？（解决A产生B？）
   - 删掉任何一章，引擎链条会断吗？（每章都有存在意义？）
   - 引擎的不可逃避性四问是否依然成立？

6. **展示方案供用户选择（必须等待用户选择，禁止自动选择）**
   - 展示每个方案的详细信息：
     - 弧线标题和章节范围
     - 引擎状态描述
     - 章节骨架（每章一句话概括）
     - 弧线质量检查结果
     - 冲突升级路径
     - 与整体故事的衔接
   - **用户可以补充或修改**：用户可以对方案提出修改意见，或补充自己的方案

7. **等待用户选择方案（禁止自动选择最优方案）**
   - 用户选择：A/B/C/用户自定义
   - 如果用户选择"用户自定义"，智能体基于用户的补充生成新方案
   - 如果用户不满意，可以重新生成方案
   - 如果用户要求深入分析某个方案，智能体展开详细分析

**关键规则**：
- ❌ **禁止**：自动选择最优分支
- ✅ **必须**：逐步推理，每步都等待用户确认或补充
- ✅ **必须**：用户可以随时补充信息
- ✅ **必须**：用户可以修改分支或提供自定义方案
- ✅ **必须**：用户要求深入分析时，智能体展开详细分析
- ❌ **禁止**：跳过用户反馈，直接生成大纲

#### 第4步：滚动式大纲生成（智能体主导，**5-10章一批**）
**必须先读取**：`references/outline_structure.md` - 了解大纲结构规范

**⚠️ 核心变化**：不一次性规划全书，5-10章一批滚动推进。前3章投入80%精力。

**滚动式生成流程**：
1. 生成当前批次的弧线概述，**展示给用户确认**
2. 用户确认后，生成章节概述（每章标注引擎状态），**展示给用户确认**
3. 用户确认后，生成详细情节点（每章至少5个，每个必须回溯引擎）
4. 用户确认后，填充辅助元素（情感变化、爽点、钩子、伏笔）
5. **每批生成完成后，执行引擎健康检查**

**每章必须标注引擎回溯**：
```
第N章：《章节标题》
引擎回溯：本章冲突源于引擎的[哪个维度]——[具体描述]
```

**引擎健康检查（每批完成后必须执行）**：
- "最近5章的冲突还来自引擎吗？"
  - 是 → 引擎运转正常，继续
  - 否 → 引擎空转了，需要引入引擎升级事件
- 引擎升级时机（当冲突开始重复时）：
  - 扩大引擎的影响范围（个人→组织→势力→天下）
  - 引入引擎的新维度
- 检查结果记入 `progress.md`

**前3章特殊处理**：
- 前3章决定读者去留，投入80%精力
- 每章生成后都要与案例对标（如果有 `style_notes.md`）
- 第1章的开场必须直接展示引擎启动

**续写大纲时**：
- 读取 `engine.md` → 恢复引擎设计
- 读取 `outline.md` → 恢复弧线规划
- 读取最近2-3章正文 → 恢复叙事节奏和悬念
- 读取 `progress.md` → 恢复用户反馈和调整方向
- 读取 `style_notes.md` → 恢复风格基准
- 生成续写部分的弧线概述、章节概述、详细情节点
- 生成完成后，**必须将续写内容合并到 `outline.md` 或用户指定的原始小说文件**
- **禁止**编辑 `/memory/user/AGENTS.md` 或其他内存文件

**用户反馈收集（每批完成后）**：
- 收集用户对这一批章节的反馈
- 记录到 `progress.md`，包括：
  - 节奏反馈（太快/太慢/刚好）
  - 引擎感受（冲突是否有趣/是否在重复）
  - 风格偏好（动作多/对话多/内心戏多）
  - 下一批的期望
- 根据反馈调整下一批的生成策略

**高效文件合并方法**：
- **精准定位最后一章**：读取文件，找到最后一章
- **找到该章节的最后位置**
- **在该章节最后换行插入**：使用 `edit_file` 工具
- **不要多次查找**：读取文件一次，精准定位

**关键规则**：
- ❌ **禁止**：一次性生成完整大纲（超过10章）
- ✅ **必须**：每步都展示给用户确认
- ❌ **禁止**：跳过用户确认，直接生成下一部分
- ✅ **必须**：每批完成后执行引擎健康检查
- ✅ **必须**：每批完成后收集用户反馈并记入 `progress.md`
- ✅ **必须**：用户确认后再继续

**质量自检**（生成时自动进行）：
- **引擎回溯检查**：每章冲突是否能回溯到引擎？
- 情节具体化：禁止模糊描述（"获得"、"遇到"等）
- 角色逻辑化：角色行为符合性格设定，不脸谱化
- 能量发现自然化：主角获得新发现/能力必须自然过渡
- 收服势力合理化：必须有合理依据，不能无脑收服
- 情报明确化：情报必须具体
- 战斗过程详细化：冲突必须有具体过程
- 技能获取常识化：符合世界观，不能无师自通
- 收服过程逻辑化：必须有逻辑过程

#### 第5步：续写大纲（如需）
如果用户要求续写大纲（如"请根据现在的大纲续写5章"、"规划第21-30章的大纲"）：

**⚠️ 重要：这是续写"大纲"，不是续写"小说正文"。大纲只规划章节结构、情节点、冲突、爽点、伏笔，不写出具体的对话和描写。**

**续写大纲遵循标准流程（第1-4步），但需要注意：**

- **第1步（世界观识别）**：如果之前已加载过，可以跳过
- **第2步（需求收集）**：从用户输入中识别文件路径和续写范围，读取现有大纲文件，提取当前状态
- **第3步（因果链推理）**：必须执行，分析续写部分的不同剧情走向，生成3个分支供用户选择
- **第4步（大纲生成）**：生成续写部分的大纲，完成后合并到用户指定的原始小说文件

**续写时的特殊要求**：
- ✅ **必须**：从用户输入中识别文件路径，使用 `read_file` 工具读取文件
- ✅ **必须**：使用用户指定的原始小说文件（如 `e:\git\deepagents-book\novels\海贼王之唯我独尊.md`）
- ✅ **必须**：禁止使用 shell 命令检查文件
- ✅ **必须**：禁止在生成分支之前创建 todo 列表或开始编写大纲
- ✅ **必须**：生成完成后，将续写内容合并到用户指定的原始小说文件
- ❌ **禁止**：读取或编辑内存文件（如 `/memory/user/AGENTS.md`）
- ❌ **禁止**：使用任何不是用户在输入中明确指定的文件路径

**文件合并操作要求**：
- ✅ **必须**：精准定位最后一章（如"第3章"或"### 第3章"）
- ✅ **必须**：找到该章节的所有内容结束位置（包括章节概述、情节点、钩子、伏笔等）
- ✅ **必须**：在该章节内容的最后一行之后，换行插入续写内容
- ✅ **必须**：使用 `edit_file` 工具，精准定位插入位置
- ❌ **禁止**：多次使用 grep 或 read_file 查找插入位置（效率低）
- ❌ **禁止**：在文件末尾插入（应该在最后一章内容结束后插入）
- ❌ **禁止**：使用 shell 命令查找文件内容或行数
- **高效方法**：读取文件一次，精准找到最后一章的所有内容结束位置，在该位置换行插入续写内容

#### 第6步：优化现有大纲（如需）
如果用户对现有大纲不满意（如"优化第5-7章"）：

**必须执行以下流程，禁止跳过**：

1. **读取现有大纲**
   - 读取要优化的章节
   - 提取当前状态：角色状态、剧情进度、伏笔

2. **确认优化需求**
   - 询问：优化哪些章节？（章节范围）
   - 询问：具体优化什么？（情节具体化、角色逻辑化、能量发现自然化等）
   - **用户可以补充信息**：用户可以主动提供优化的具体要求

3. **执行因果链推理，生成优化分支**（必须，禁止跳过）
   - **优化大纲时，必须执行因果链推理**，分析优化的不同方向和策略
   - 即使不涉及明确的剧情改变，也要分析不同的优化路径
   - 按照第3步（因果链推理）的完整流程执行：
     - **分析优化的可能方向**：
       - 分析当前问题章节的多种改进可能性
       - 分析不同优化策略（保守/平衡/激进）的影响
       - 分析优化后的蝴蝶效应和连锁反应
     - **逐步推理，每步都等待用户确认或补充**：
       - 第1步：分析优化部分的核心问题和可能改进方向
       - 第2步：分析不同优化策略的影响
       - 第3步：分析优化后的蝴蝶效应和连锁反应
       - 第4步：分析世界修正机制
       - 用户可以随时补充信息或要求深入分析
     - **生成3个分支（必须生成）**：
       - 分支A：保守优化（最小改动，保持原框架）
       - 分支B：平衡优化（适度改进，平衡改动）
       - 分支C：激进优化（大幅改进，可能改变框架）
       - 每个分支包含：优化策略、详细描述、影响分析、风险收益等
     - **展示分支供用户选择（必须等待用户选择）**：
       - 展示每个分支的详细信息
       - 用户可以修改分支或提供自定义方案
       - **禁止自动选择**，必须等待用户选择

4. **智能体主导优化**
   - 针对问题章节应用8项优化原则
   - 情节具体化：将模糊描述转化为具体行动
   - 角色逻辑化：确保角色行为符合性格设定
   - 能量发现自然化：主角获得新发现自然过渡
   - 收服势力合理化：必须有合理依据
   - 情报明确化：情报必须具体
   - 战斗过程详细化：冲突必须有具体过程
   - 技能获取常识化：符合世界观
   - 收服过程逻辑化：必须有逻辑过程

5. **可调用脚本辅助**（可选）：
   ```bash
   python scripts/storyline_redesigner.py \
     --input /path/to/outline.md \
     --scope "chapters 5-7" \
     --requirement "情节具体化" \
     --output /path/to/new_outline.md
   ```
   - 脚本提供：当前状态分析、连贯性报告
   - 智能体基于脚本输出进行优化创作

6. **渐进式生成优化内容（强制渐进式交互）**
   - 第一步：生成优化后的章节概述，**展示给用户确认**
   - 用户确认后，第二步：生成优化后的详细情节点
   - 质量自检：自动检查8项优化原则

**关键规则**：
- ✅ **必须**：读取现有大纲，提取当前状态
- ✅ **必须**：执行因果链推理，分析优化的不同方向和策略，生成3个分支供用户选择
- ✅ **必须**：等待用户选择分支后，再开始优化
- ✅ **必须**：应用8项优化原则
- ✅ **必须**：渐进式生成，每步都展示给用户确认
- ❌ **禁止**：跳过因果链推理，直接优化大纲
- ❌ **禁止**：跳过生成分支，直接生成优化内容
- ❌ **禁止**：跳过用户选择分支，自动选择最优分支
- ❌ **禁止**：跳过用户确认，直接修改大纲
- ❌ **禁止**：重新设计整个故事线（除非用户明确要求）

#### 第7步：输出结果
- 生成完整的大纲文件（Markdown格式）
- 包含：卷概述、章节概述、情节点、情感变化、爽点、钩子、伏笔
- 保存到用户指定路径或当前工作目录

---

## 因果链动态推理（核心能力）

当用户提出改变需求时，智能体自动执行因果链推理，支持蝴蝶效应分支生成。

### 推理模式（默认使用交互式逐步推理）

智能体在因果链推理时，**必须使用交互式逐步推理模式**。

#### 模式1：全自动模式（❌ 禁止使用）
- 用户输入需求
- 智能体自动推理、选择最优分支、生成大纲
- **禁止使用场景：用户未明确要求全自动**

#### 模式2：半自动模式（默认推荐）
- 用户输入需求
- 智能体逐步推理，**每步都等待用户确认或补充**
- 基于推理结果和用户反馈，生成3个分支
- 等待用户选择分支（或提供自定义方案）
- **适用场景：大多数情况，用户希望控制剧情走向**

#### 模式3：交互式模式（用于复杂剧情改变）
- 用户输入需求
- 智能体逐步推理，**每步都等待用户确认或补充**
- 用户可以随时打断推理，补充自己的想法
- 用户可以要求跳过某些推理步骤
- 用户可以要求深入分析某个环节
- **适用场景：重大剧情改变、复杂蝴蝶效应**

**关键规则**：
- 默认使用**半自动模式**：逐步推理，每步都等待用户反馈
- **用户可以随时补充信息**：用户可以主动提供额外的设定或约束
- **用户可以修改分支**：用户可以对分支提出修改意见，或提供自定义方案
- **用户可以要求深入分析**：智能体必须展开详细分析
- 禁止使用**全自动模式**：除非用户明确要求"你直接生成"

### 推理流程（半自动模式）

#### 第1步：理解需求
智能体解析用户需求：
- **改变类型**：收服角色/改变事件/影响势力/获得能力
- **改变粒度**：单事件/章节范围/整卷
- **改变对象**：原作角色（路飞、索隆、娜美等）/势力/地点

#### 第2步：加载状态
智能体从知识库加载：
- 原作剧情：相关章节的原作情节
- 世界状态：角色状态（能力、性格、关系）、势力状态、地点状态
- 状态依赖：原作事件触发的前提条件和影响后果

#### 第3步：因果推理
智能体计算：
- **直接影响**：改变事件对世界的直接影响
- **连锁反应**（至少3层）：
  - 第1层：直接影响相关角色
  - 第2层：影响后续事件
  - 第3层：影响长期剧情
- **生成3个分支**：每个分支都是可能的剧情走向

#### 第4步：一致性校验
对每个分支，智能体检查：
- 角色行为是否符合性格？（不能脸谱化）
- 能力获取是否有合理来源？（不能无师自通）
- 势力变化是否有逻辑依据？（不能无脑收服）
- 新情节是否逻辑自洽？（有迹可循）

#### 第5步：分支详细展示
智能体向用户展示3个分支的详细信息，包含以下维度：

**分支基本信息**：
- 分支标题：简要描述分支核心
- 策略类型：保守/平衡/激进
- 详细描述：分支的具体执行方案

**世界修正信息**：
- 修正类型：人物替代/情节补偿/运气加持/角色行为引导
- 修正强度：弱/中/强
- 修正描述：世界如何自动修正剧情

**蝴蝶效应列表**：
- 改变点：主角改变的具体行为
- 连锁反应：改变引发的连锁反应（至少3层）
- 长期影响：对后续剧情的影响

**成功率分析**：
- 成功率百分比：0-100%
- 难度等级：简单/中等/困难/极难
- 成功率原因：基于原作设定、主角实力、机遇等因素分析

**风险分析**：
- 风险列表：详细列出每个分支可能面临的风险
- 风险评估：基于角色性格、世界修正、主角行为等维度评估

**收益分析**：
- 收益列表：详细列出每个分支的潜在收益
- 收益评估：基于战力提升、关系改善、剧情控制等维度评估

**时间线分析**：
- 时间线节点：列出分支的关键时间节点（章节范围）
- 剧情节奏：帮助用户了解分支的剧情节奏

**一致性校验结果**：
- 是否通过一致性校验
- 如果未通过，列出具体问题

用户根据以上信息选择分支（A/B/C）。

#### 第6步：生成大纲
根据用户选择的分支，智能体生成详细大纲：
- 标注改变点
- 标注连锁反应
- 标注蝴蝶效应
- 标注主角vs天意的博弈过程

### 主角行为逻辑原则

在生成分支时，智能体必须遵循以下原则：

#### 1. 对敌人：斩尽杀绝
- 主角对敌人的处理必须是彻底清除
- 不能留后患，不能网开一面

#### 2. 对路人/无冤无仇者：不主动阻挠
- 如果主角已经"偷偷摸摸抢了路飞的机缘"（如收服索隆）
- 那么主角不应该继续阻挠世界修正（如克洛加入路飞）
- 逻辑：已经占了便宜，不贪心

#### 3. 分支的多样性
- 收服不了索隆、收服路飞、收服其他角色，都是可能的分支
- 不应该预设主角一定能成功
- 要根据主角实力和机遇，合理设计成功率

---

## 优化模式（核心改进点）

当优化现有大纲时，必须遵循以下改进原则：

### 一、叙事结构原则（最重要）⭐

> 参考 [大纲结构规范](references/outline_structure.md) 中的"叙事结构规范"章节

#### 1. 起承转合完整化
- **原则**：每卷必须有完整的叙事弧线，不能一直"爽"或一直"抑"
- **比例**：起(20%) → 承(40%) → 转(25%) → 合(15%)
- **示例**：
  - ❌ 错误：第1-20章主角一路开挂，无往不利
  - ✅ 正确：第1-4章建立目标 → 第5-12章积累冲突 → 第13-17章陷入危机 → 第18-20章逆转成长

#### 2. 抑扬节奏平衡化
- **原则**：没有"抑"就没有"扬"的重量，抑扬比例约4:6
- **禁止**：
  - 连续3章以上"释放章"（读者会腻）
  - 连续5章以上"蓄势章"（读者会弃书）
- **示例**：
  - ❌ 错误：打脸 → 打脸 → 打脸 → 打脸
  - ✅ 正确：小胜 → 遭遇更强敌人 → 陷入绝境 → 绝地反击

#### 3. 角色弧光必备化
- **原则**：主角在每卷必须有内在成长，不只是外在收获
- **模型**：缺陷状态 → 改变事件 → 成长状态
- **示例**：
  - ❌ 错误：李寒收服索隆，势力+1（只有外在）
  - ✅ 正确：李寒原本只把人当棋子 → 索隆为他挡刀 → 学会珍惜同伴

#### 4. 灵魂黑夜必备化
- **原则**：每卷必须有主角的最低谷时刻，让高潮更有重量
- **示例**：
  - ❌ 错误：主角遇到困难，轻松解决
  - ✅ 正确：主角失去一切（地位、同伴、信念），在绝望中找到新的力量

#### 5. 核心张力明确化
- **原则**：每章必须有困境或抉择，不能是单纯的"主角赢了"
- **好的张力**：两难抉择、代价与收获的权衡
- **示例**：
  - ❌ 错误："李寒打败了老鼠"（无张力）
  - ✅ 正确："李寒必须选择：暴露身份救索隆，还是隐忍等待更好时机"

### 二、情节逻辑原则

#### 6. 情节具体化
- **原则**：模糊描述必须转化为具体行动
- **示例**：
  - ❌ 错误："李寒获得军功"
  - ✅ 正确："李寒提供老鼠的'内部情报'撬开俘虏的口，蒙卡记一等功"

#### 7. 角色逻辑化
- **原则**：角色行动必须符合其性格设定，不能脸谱化
- **示例**：
  - ❌ 错误："老鼠直接派人暗杀李寒"
  - ✅ 正确："老鼠生性多疑，先派人试探李寒是否真的背叛"

#### 8. 代价意识化 ⭐新增
- **原则**：成功必须有代价，选择必须有牺牲
- **示例**：
  - ❌ 错误："李寒轻松收服索隆"
  - ✅ 正确："李寒为救索隆暴露了身份，失去了在海军的隐蔽优势"

#### 9. 能力获取自然化
- **原则**：主角获得能力必须有合理过程，不能开天眼
- **示例**：
  - ❌ 错误："李寒看索隆练剑，学会了他的剑术"
  - ✅ 正确："李寒拜师耕四郎，经过一个月学习，获得剑术入门"

#### 10. 收服过程逻辑化
- **原则**：收服强敌必须有逻辑过程，不能一蹴而就
- **标准流程**：了解需求 → 提供价值 → 建立信任 → 经历考验 → 真正认可

---

## 资源索引

### 创意引擎文档（第0步必须读取）
- **创意脑洞+引擎方法论**：见 [references/creative_hooks.md](references/creative_hooks.md)
  - 用途：12种脑洞类型、不可逃避性四问检验、引擎设计三要素、常见失败模式
  - 何时读取：**第0步（脑洞+引擎设计）时必须读取**
- **标题工程指南**：见 [references/title_engineering.md](references/title_engineering.md)
  - 用途：冒号标题公式、五大标题模式、标题自检四问
  - 何时读取：**第0步设计标题时必须读取**
- **成功案例拆解**：见 [references/successful_premises.md](references/successful_premises.md)
  - 用途：成功网文的脑洞→引擎→不可逃避性→升级路径深度拆解
  - 何时读取：**第0步对标引擎强度时必须读取**
- **世界观语言风格指南**：见 [references/world_language_styles.md](references/world_language_styles.md)
  - 用途：6种世界类型的语言寄存器、穿越者语言适配三阶段
  - 何时读取：**确定世界观后读取，确保语言风格匹配**

### 核心规范文档（必须读取）
- **大纲结构规范**：见 [references/outline_structure.md](references/outline_structure.md)
  - 用途：了解大纲结构、卷概述规范、章节概述规范、情节点规范、辅助元素规范
  - 何时读取：**每次生成/优化大纲前必须读取**
- **情节逻辑指南**：见 [references/plot_logic.md](references/plot_logic.md)
  - 用途：情节设计基本原则、情节逻辑验证清单、常见逻辑错误及修正
  - 何时读取：需要确保情节有迹可循时读取
- **角色塑造指南**：见 [references/character_guide.md](references/character_guide.md)
  - 用途：角色塑造基本原则、角色性格模板、角色行为逻辑、角色互动模式
  - 何时读取：需要塑造立体角色时读取

### 世界观知识库（根据同人类型动态加载）
- **海贼王**：见 [references/onepiece_knowledge_base.md](references/onepiece_knowledge_base.md)
  - 用途：了解海贼王原作设定（角色、能力、剧情、势力等）
  - 何时读取：**写海贼王同人时必须首先读取**，只读取一次就记住
- **火影**：见 [references/naruto-world.md](references/naruto-world.md)
  - 用途：了解火影原作设定
  - 何时读取：**写火影同人时必须首先读取**，只读取一次就记住
- **死神**：见 [references/bleach-world.md](references/bleach-world.md)
  - 用途：了解死神原作设定
  - 何时读取：**写死神同人时必须首先读取**，只读取一次就记住

### 参考模板
- **角色模板**：见 [references/character-template.md](references/character-template.md)
  - 用途：创建角色卡时的参考格式
- **大纲模板**：见 [references/outline-template.md](references/outline-template.md)
  - 用途：参考完整的大纲示例

### 辅助脚本（智能体可选调用，提供结构化支持）

**注意**：这些脚本提供结构化数据处理，**核心创作由智能体主导**

- **因果链推理引擎**：见 [scripts/causal_chain_reasoner.py](scripts/causal_chain_reasoner.py)
  - 功能：状态追踪、连锁反应计算、分支生成、一致性校验、成功率/风险/收益/时间线分析
  - 用途：当用户提出改变原作剧情时（如"收服索隆"），智能体可调用此脚本获取结构化数据，然后基于数据进行推理和分支选择
  - 调用示例：
    ```bash
    python scripts/causal_chain_reasoner.py "收服索隆"
    ```

- **故事线重新设计器**：见 [scripts/storyline_redesigner.py](scripts/storyline_redesigner.py)
  - 功能：解析现有大纲、提取当前状态（角色、伏笔）、连贯性检查
  - 用途：当用户需要重新设计故事线时，智能体可调用此脚本获取现有大纲的结构化数据
  - 调用示例：
    ```bash
    python scripts/storyline_redesigner.py \
      --input /path/to/outline.md \
      --scope "chapters 10-20" \
      --requirement "改为收服克利" \
      --output /path/to/new_outline.md
    ```

### 输出资产
- **案例研究**：见 [assets/case-studies.md](assets/case-studies.md)
  - 用途：参考蝴蝶效应推理示例、大纲质量标准

---

## 中断恢复机制

当用户退出后回来续写时，必须执行以下恢复流程：

1. **读取 `engine.md`** → 恢复引擎设计（脑洞、引擎、弧线骨架）
2. **读取 `outline.md`** → 恢复弧线规划和已有章节
3. **读取最近2-3章正文**（如果有）→ 恢复叙事节奏和悬念
4. **读取 `progress.md`** → 恢复用户反馈和调整方向
5. **读取 `style_notes.md`**（如果有）→ 恢复风格基准和案例参考

恢复后，向用户确认：
- 当前进度（已完成到第几章）
- 引擎状态（引擎是否还在正常运转）
- 上次的用户反馈和调整方向
- 下一批的计划

---

## 注意事项

- **触发条件**：满足以下任一条件即触发 Skill（生成大纲/续写大纲/优化大纲/改变剧情/蝴蝶效应推理）
- **续写大纲 vs 续写正文**：
  - ✅ **续写大纲**：规划章节结构、情节点、冲突、爽点、伏笔（会触发本Skill）
  - ❌ **续写正文**：写出具体的对话、描写、场景（不会触发本Skill）
  - **关键区别**：大纲是"规划"，正文是"写作"
- **续写和优化必须执行完整流程**：续写和优化时，必须重新执行 Skill 的完整流程（世界观加载、因果链推理、质量检查），确保一致性
- **智能体工具系统**：使用专用工具管理创作状态

  **角色管理（推荐使用专用工具）**：
  - `update_character("索隆", status="已收服", location="东海", note="第5章收服")` - 更新角色状态
  - `get_character("索隆")` - 获取角色详情
  - `list_characters("已收服")` - 列出指定状态的角色
  - `add_relationship("李寒", "索隆", "上下级", mutual=True)` - 添加角色关系

  **伏笔管理（推荐使用专用工具）**：
  - `plant_foreshadow("老鼠交易", "老鼠与海贼有交易", chapter=3, target_chapter=10)` - 埋设伏笔
  - `resolve_foreshadow("老鼠交易", resolved_chapter=10, resolution="揭示真相")` - 回收伏笔
  - `list_foreshadows()` - 查看待回收伏笔

  **大纲完成（强烈推荐）**：
  - `complete_outline(chapter, outline, title?)` - 完成大纲（自动保存+更新进度）

  **进度管理**：
  - `get_progress()` - 查看当前进度
  - `update_progress(outline_completed=10)` - 手动更新进度

  **基础记忆工具（通用）**：
  - `remember("decision", "收服策略", "用户选择平衡策略")` - 记住用户决策
  - `recall("decision")` - 回忆决策信息
  - `forget("decision", "已废弃选择")` - 删除过时信息

- **用户可以覆盖记忆**：如果用户希望改变某个设定，使用相应工具更新
- **世界观加载**：写同人小说时必须首先加载对应的世界观知识库（海贼王/火影/死神），确保 AI 不胡编乱造
- **对话式交互（强制）**：通过对话从用户处逐步获取信息，每次只问1-2个问题，等待用户回答后再问下一个
- **渐进式生成（强制）**：大纲生成遵循"渐进式披露"原则（卷概述→章节概述→详细情节点），每步都必须展示给用户确认
- **因果链推理（强制交互式逐步推理）**：
  - **逐步推理**：每步都等待用户确认或补充
  - **用户可以随时补充**：用户可以主动提供额外的设定或约束
  - **用户可以修改分支**：用户可以对分支提出修改意见，或提供自定义方案
  - **用户可以要求深入分析**：智能体必须展开详细分析
  - **禁止全自动模式**：除非用户明确要求"你直接生成"
- **智能体主导**：核心创作（大纲生成、优化、因果推理）由智能体完成，脚本仅提供结构化支持
- **质量自检**：生成时自动检查10项优化原则（含叙事结构5项+情节逻辑5项）
- **蝴蝶效应标注**：生成大纲时，必须标注改变点、连锁反应、蝴蝶效应
- **分支多样性**：收服不了索隆、收服路飞、收服其他角色，都是可能的分支，不能预设主角一定能成功
- **最小化改动**：优化现有大纲时，只修改问题章节，不要重新设计整个故事线

### ⭐ 叙事结构核心原则（新增）

- **起承转合（强制）**：每卷必须有完整的叙事弧线
  - 起(20%)：建立世界、角色、目标
  - 承(40%)：发展冲突、积累张力
  - 转(25%)：重大转折、危机
  - 合(15%)：解决冲突、角色成长
- **抑扬结合（强制）**：不能一味"爽"，必须有低谷衬托高潮
  - 抑:扬 ≈ 4:6（先苦后甜，但苦要够苦）
  - 禁止连续3章以上"释放章"
  - 禁止连续5章以上"蓄势章"
- **角色弧光（强制）**：每卷主角必须有内在成长，不只是外在收获
  - 模型：缺陷状态 → 改变事件 → 成长状态
- **灵魂黑夜（强制）**：每卷必须有主角的最低谷时刻
- **核心张力（强制）**：每章必须有困境或抉择，不能只是"主角赢了"
- **代价意识（强制）**：成功必须有代价，选择必须有牺牲

---

## 使用示例

### 示例1：生成海贼王同人大纲（交互式逐步推理）

**用户**：帮我生成一个海贼王同人小说大纲，要收服索隆

**Agent 工作流程**：

1. **识别同人类型，加载世界观知识库**（必须首先执行）
   ```
   read_file references/onepiece_knowledge_base.md
   ```
   - 目的：确保 AI 不胡编乱造，严格遵循海贼王原作设定

2. **对话式收集信息（每次只问1-2个问题）**
   - Agent：请告诉我主角的姓名、性格、金手指类型？
   - 用户：李寒，冷静果断，加点系统
   - Agent：主角的起点是什么？（海军/海贼/其他）
   - 用户：从海军三等兵开始
   - Agent：主角的目标是什么？
   - 用户：成为海军元帅
   - Agent：关于收服索隆，您有什么具体想法吗？比如时机、策略？
   - 用户：在谢尔兹镇事件时收服，用情报换取索隆的信任

3. **因果链推理（交互式逐步推理，每步都等待用户反馈）**

   **第1步：分析直接影响**
   - Agent：收服索隆会导致索隆不会加入路飞，路飞失去剑士，战斗力大幅下降
   - Agent：索隆的"世界第一剑豪"目标会受到影响，可能会调整目标
   - Agent：请问您如何看待这个影响？是否接受？
   - 用户：接受，索隆会继续追求世界第一剑豪

   **第2步：分析第1层连锁反应**
   - Agent：路飞失去剑士，在巴拉蒂餐厅对抗克利克时会非常吃力，可能重伤
   - Agent：世界可能会安排其他剑士加入路飞，或者给路飞更强的能力
   - Agent：请问您希望世界如何修正？还是让路飞自己克服？
   - 用户：让世界自动修正，看会出现什么情况

   **第3步：分析第2层连锁反应**
   - Agent：如果路飞在巴拉蒂重伤，可能无法正常前往可可亚西村
   - Agent：阿龙事件可能会推迟，或者娜美被迫独自对抗阿龙
   - Agent：这会影响娜美对路飞的信任，娜美可能不会那么容易信任路飞
   - Agent：请问您希望如何处理？可以让索隆暗中帮助路飞吗？
   - 用户：可以，让索隆偶尔暗中帮助，但不直接暴露身份

   **第4步：分析第3层连锁反应**
   - Agent：长期来看，路飞的团队会变化，战斗力构成也会变化
   - Agent：可能在伟大航路遇到不同的伙伴
   - Agent：您希望路飞的团队如何发展？
   - 用户：正常发展，只是少了索隆

   **第5步：分析世界修正**
   - Agent：世界可能会安排克洛或者其他剑士加入路飞
   - Agent：也可能会给路飞更强的霸气或果实能力
   - Agent：您希望世界如何修正？
   - 用户：让世界自然修正，不干预

4. **生成3个分支（基于用户反馈调整）**
   - Agent：基于以上分析和您的反馈，我生成3个分支供您选择：

   **分支A：保守策略（成功率30%，风险小）**
   - 收服索隆后，让他暗中帮助路飞，不直接暴露身份
   - 路飞团队正常发展，只是少了索隆
   - 世界安排克洛或其他剑士加入路飞
   - 收益：索隆成为您的部下，获得顶尖剑士
   - 风险：路飞战斗力下降，可能影响后续剧情

   **分支B：平衡策略（成功率50%，风险中等）**
   - 收服索隆后，让他偶尔暗中帮助路飞
   - 路飞团队正常发展，在伟大航路遇到新的伙伴
   - 世界给路飞更强的霸气
   - 收益：索隆成为您的部下，路飞继续成长
   - 风险：世界修正可能不可控

   **分支C：激进策略（成功率20%，风险大）**
   - 收服索隆后，让索隆完全脱离路飞
   - 路飞在东海遇到重大困难，被迫快速成长
   - 世界给路飞霸王色霸气觉醒
   - 收益：索隆完全属于您，路飞快速成长
   - 风险：路飞可能夭折，蝴蝶效应严重

5. **用户选择或补充**
   - 用户：我想要分支B，但是索隆不能完全隐藏，要在关键时刻帮助路飞
   - Agent：好的，我调整分支B，加入索隆在关键时刻帮助路飞的设定

6. **根据用户选择生成大纲（渐进式生成，每步都等待用户确认）**
   - 读取大纲结构规范：
     ```
     read_file references/outline_structure.md
     ```
   - 渐进式生成：
     - 第一步：生成卷概述，**展示给用户确认**
     - 用户确认后，第二步：生成章节概述，**展示给用户确认**
     - 用户确认后，第三步：生成详细情节点
   - 质量自检：自动检查8项优化原则

7. **输出结果**
   ```
   write_file 海贼之唯我独尊-大纲.md
   ```

### 示例3：续写后续章节（交互式逐步推理）

**用户**：续写第21-30章，路飞团队到了伟大航路

**Agent 工作流程**：

1. **读取现有大纲**
   ```
   read_file 海贼之唯我独尊-大纲.md
   ```
   - 提取当前状态：主角军衔（上校）、已收服势力、已执行的蝴蝶效应

2. **确认续写范围**
   - Agent：您要续写第21-30章，有什么具体要求吗？
   - 用户：让主角收服娜美
   - Agent：收服娜美会影响路飞的航海士，路飞在伟大航路会非常困难...
   - Agent：请问您希望如何处理？

3. **因果链推理（交互式逐步推理）**

   **第1步：分析直接影响**
   - Agent：娜美不会加入路飞，路飞失去航海士，在伟大航路会迷失方向...
   - Agent：请问您如何看待这个影响？
   - 用户：接受，让世界安排新的航海士

   **第2步：分析连锁反应**
   - Agent：路飞可能会在伟大航路遇到新的航海士...
   - Agent：您希望世界如何修正？
   - 用户：自然修正，不干预

4. **生成续写大纲（渐进式生成，每步都等待用户确认）**
   - 第一步：生成卷概述（续写部分），**展示给用户确认**
   - 用户确认后，第二步：生成章节概述，**展示给用户确认**
   - 用户确认后，第三步：生成详细情节点

5. **合并到大纲文件**
   ```
   edit_file 海贼之唯我独尊-大纲.md
   ```

### 示例4：优化现有大纲（交互式逐步推理）

**用户**：优化第5-7章，让情节更具体

**Agent 工作流程**：

1. **读取现有大纲**
   ```
   read_file 海贼之唯我独尊-大纲.md
   ```
   - 提取第5-7章的当前状态

2. **确认优化需求**
   - Agent：您希望优化哪些方面？（情节具体化/角色逻辑化/能量发现自然化等）
   - 用户：情节具体化，把"李寒获得军功"改为具体行动

3. **智能体主导优化**
   - 针对第5-7章应用8项优化原则
   - 情节具体化：将"李寒获得军功"改为"李寒提供老鼠的'内部情报'撬开俘虏的口，蒙卡记一等功"

4. **渐进式生成优化内容（每步都等待用户确认）**
   - 第一步：生成优化后的章节概述，**展示给用户确认**
   - 用户确认后，第二步：生成优化后的详细情节点

5. **更新大纲文件**
   ```
   edit_file 海贼之唯我独尊-大纲.md
   ```

**用户**：优化第5-7章，让情节更具体

**Agent 工作流程**：

1. 读取现有大纲
   ```
   read_file 海贼之唯我独尊-大纲.md
   ```

2. 读取情节逻辑指南
   ```
   read_file references/plot_logic.md
   ```

3. **智能体主导优化**：
   - 针对第5-7章应用8项优化原则
   - 情节具体化：将模糊描述转化为具体行动
   - 角色逻辑化：确保角色行为符合性格设定
   - 能量发现自然化：主角获得新发现自然过渡
   - 其他优化...

4. **可调用脚本辅助**（可选）：
   - 如果需要重新设计：
     ```bash
     python scripts/storyline_redesigner.py \
       --input 海贼之唯我独尊-大纲.md \
       --scope "chapters 5-7" \
       --requirement "让情节更具体" \
       --output 海贼之唯我独尊-优化版.md
     ```
   - 脚本提供：当前状态分析、连贯性报告
   - 智能体基于脚本输出进行优化创作

5. **输出结果**
   ```
   edit_file 海贼之唯我独尊-大纲.md
   ```

### 示例3：因果链推理（改变剧情）

**用户**：如果收服索隆，会有什么影响？

**Agent 工作流程**：

1. **识别同人类型，加载世界观知识库**（如果尚未加载）
   ```
   read_file references/onepiece_knowledge_base.md
   ```

2. **智能体主导推理**：
   - 理解改变需求：收服索隆
   - 分析直接影响：路飞失去剑士
   - 分析连锁反应（3-5层）

3. **调用脚本辅助**：
   ```bash
   python scripts/causal_chain_reasoner.py "收服索隆"
   ```

4. **脚本输出**（结构化数据）：
   - 改变事件分析
   - 连锁反应（3-5层）
   - 3个分支的详细信息：
     - 分支A：收服索隆成功（保守策略，成功率18%）
     - 分支B：收服索隆，主动帮助路飞（平衡策略，成功率36%）
     - 分支C：收服索隆，阻挠路飞（激进策略，成功率12%）
   - 每个分支包含：成功率、风险列表、收益列表、时间线、一致性校验结果

5. **智能体展示推理结果**：
   - 将脚本的结构化输出转化为用户友好的说明
   - 展示3个分支供用户选择
   - 根据用户选择生成后续剧情
