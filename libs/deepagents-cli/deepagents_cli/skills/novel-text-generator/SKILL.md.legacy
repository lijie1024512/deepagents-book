---
name: novel-text-generator
description: 用于根据大纲生成高质量小说正文的Skill。以创意引擎为核心意识，能够根据已有的章节大纲，生成文笔优美、人物鲜活、节奏适当的小说正文。支持引擎意识正文生成、案例对标、前3章多版本精磨、渐进式场景生成、中断恢复续写、文笔提升机制、角色声音系统和爽点放大技巧。
dependency:
  python:
    - pyyaml>=6.0
---

# 小说正文生成器

## 与大纲Skill的关系

### 基本关系

| 维度 | 大纲Skill | 正文Skill |
|------|-----------|-----------|
| 核心任务 | 规划故事（情节、结构、逻辑） | 讲述故事（描写、对话、文笔） |
| 回答问题 | **发生什么？为什么？** | **读者感受到什么？** |
| 输出形式 | 情节点、核心张力、情感曲线、角色弧光 | 具体场景、对话、描写、内心戏 |
| 字数级别 | 每章 300-500 字（详细大纲） | 每章正文 3000-8000 字 |

### 直接转化关系（无细纲）

本项目采用**大纲直接转正文**模式，因此大纲必须足够详细：

```
大纲（详细） ────────────────→ 正文
    │                            │
    │  不经过细纲                 │
    │                            │
    ├─ 情节点(7个)               ├─ 场景(7-10个)
    ├─ 核心张力                  ├─ 情感体验
    ├─ 情感曲线                  ├─ 节奏控制
    ├─ 三层冲突                  ├─ 潜台词/行动
    └─ 角色弧光片段              └─ 内心戏/moment
```

### 大纲元素与正文元素的对应

| 大纲元素 | 正文转化目标 | 转化方式 |
|---------|-------------|---------|
| **情节点** | 场景 | 1个情节点 = 1-2个场景 |
| **核心张力** | 情感体验 | 用场景、对话、行动让读者**感受到**抉择的困难 |
| **情感曲线** | 节奏控制 | 低谷=慢节奏+压抑描写；高峰=快节奏+释放描写 |
| **三层冲突** | 潜台词/行动 | 内在冲突=内心独白；人际冲突=对话潜台词；外在冲突=环境压迫 |
| **角色弧光片段** | 关键moment | 用具体的行动/抉择展现角色内在变化 |
| **章节节奏(蓄势/爆发/释放)** | 场景密度+字数分配 | 蓄势=慢镜头；爆发=快剪辑；释放=情感宣泄 |

---

## 触发场景示例

### ✅ 会触发本Skill的场景

1. **根据大纲写正文**
   - 用户："根据大纲写第X章正文"
   - 用户："把第3章的大纲展开成正文"
   - 用户："生成第5章的详细内容"

2. **续写正文**
   - 用户："续写第4章的内容"
   - 用户："继续写下去"
   - 用户："把这个场景写完"

3. **优化正文**
   - 用户："优化这段描写"
   - 用户："重写这段对话"
   - 用户："让这个场景更生动"

4. **修改正文风格**
   - 用户："让战斗场景更激烈"
   - 用户："增加感官描写"
   - 用户："让角色对话更有特色"

### ❌ 不会触发本Skill的场景

1. **规划大纲**
   - 用户："帮我生成一个小说大纲"（这是大纲Skill的任务）
   - 用户："续写第21-30章的大纲"（这是大纲Skill的任务）
   - 用户："优化大纲的情节点"（这是大纲Skill的任务）

**关键区别**：
- ✅ **正文**：写出具体的对话、描写、场景、心理活动
- ❌ **大纲**：规划章节结构、情节点、冲突、爽点、伏笔

---

## 任务目标

- 本Skill用于：根据大纲生成、续写、优化高质量的小说正文
- 能力包含：场景描写、对话写作、心理刻画、节奏控制、爽点放大、文笔提升
- 触发条件（满足以下任一条件即触发）：
  - **生成正文**：用户提供大纲或指定章节，要求写正文
  - **续写正文**：用户要求"续写"、"继续写"、"写完这个场景"等
  - **优化正文**：用户要求"优化描写"、"重写对话"、"让场景更生动"等

---

## ⚠️ 重要：强制交互式要求

**在使用本技能时，必须严格遵守以下规则，禁止跳过任何步骤：**

### ⚠️ 文件操作要求

- ✅ **必须**：使用 `read_file` 工具读取大纲文件和已有正文
- ❌ **禁止**：使用 shell 命令检查文件
- ✅ **必须**：每个场景生成后等待用户确认
- ❌ **禁止**：一次性生成整章正文

### 核心要求：渐进式场景生成

**无论是生成新正文、续写正文还是优化正文，都必须遵循渐进式场景生成，每个场景都需要用户确认。**

- ✅ **生成正文**：必须按场景逐步生成，每个场景等待用户确认
- ✅ **续写正文**：必须从断点处继续，按场景逐步生成
- ✅ **优化正文**：必须展示优化前后对比，等待用户确认

**禁止一次性生成整章正文，容易导致质量下降。**

---

## 引擎意识（核心新增）⭐

### 什么是引擎意识

正文生成不只是把大纲"展开成文字"，而是要让读者在每一章都**感受到引擎的存在**——那个贯穿全书、不可逃避的核心矛盾。

**生成正文前，必须读取 `engine.md`**，理解：
- 当前章节在引擎中的位置（引擎刚启动？加速中？即将反噬？）
- 本章冲突如何源于引擎（不是随机事件）
- 引擎的代价在本章如何体现

### 引擎意识的正文体现

| 引擎阶段 | 正文氛围 | 描写重点 |
|---------|---------|---------|
| 引擎启动 | 不安、暗涌 | 暗示性描写——让读者隐约感觉"有什么不对" |
| 引擎加速 | 压迫、紧张 | 冲突逼近的感官描写——时间、距离、威胁 |
| 引擎反噬 | 绝望、黑暗 | 代价的痛苦描写——失去、牺牲、无力 |
| 引擎升级 | 震撼、新恐惧 | 更大维度冲突的初现——世界观扩展 |

### 示例

**引擎**：吃了手术果实，罗在追杀

**引擎启动阶段的正文**（隐约的不安）：
```
李寒低头看着自己的手掌。一种陌生的力量在血管中游走，像是活的，有自己的意志。

他握紧拳头，又松开。

能力是好东西。但他总觉得，用了这个能力，就像是在黑暗中点燃了一根火柴——
能看清眼前，却也让远处的某些东西，看清了你。
```

**引擎加速阶段的正文**（压迫感）：
```
"北海方向有人使用了Room。"

电话虫那头沉默了三秒。

"确认了吗？"

"确认了。信号特征和'手术果实'完全匹配。"

三千海里外，特拉法尔加·罗缓缓站了起来。他的眼中没有愤怒，只有一种
冰冷到骨子里的决心。

"那是我的东西。"
```

---

## 案例对标机制（新增）

### 何时使用案例对标

- 写关键段落时（开场、高潮、转折、结尾）
- 写前3章时（必须对标）
- 用户要求提升文笔时
- 引擎关键时刻（启动、反噬、升级）

### 对标方式

如果项目中有 `style_notes.md`（记录了案例索引）：
1. 读取 `style_notes.md`，找到当前场景类型对应的案例参考
2. 读取案例的对应段落（如果是本地文件）或回忆案例风格
3. 在生成时说"参考这个水平"，确保文笔质量对标

**对标不是抄袭**，而是：
- 对标**文笔水平**——描写的细腻度、节奏的控制力
- 对标**情感力度**——读者在这个场景应该有多强的感受
- 对标**角色味道**——对话的质感、人物的灵魂

---

## 前3章多版本机制（核心新增）⭐

### 为什么前3章要特殊处理

前3章决定读者去留。投入80%精力在前3章，是最有性价比的策略。

### 多版本生成流程

**第1章**（必须多版本）：
1. AI生成3个版本（不同开场方式、不同叙事节奏、不同首句）
2. 用户选最好的，指出"这里好、这里不好"
3. AI根据反馈改进 → 再出2个改进版
4. 用户确认最终版

**第2章**（推荐多版本）：
- 至少生成2个版本
- 用户选择并反馈

**第3章**（推荐多版本）：
- 至少生成2个版本
- 用户选择并反馈

**第4章起**：
- 恢复单版本模式（除非用户要求多版本）
- 5章一批滚动推进

### 版本差异化维度

| 维度 | 版本A | 版本B | 版本C |
|------|-------|-------|-------|
| 开场方式 | 悬念开场 | 感官开场 | 对话开场 |
| 叙事节奏 | 快节奏 | 慢节奏 | 中等节奏 |
| 首句风格 | 短句冲击 | 环境铺陈 | 角色独白 |
| 引擎展示 | 直接展示冲突 | 暗示性铺垫 | 角色视角感知 |

---

## 前置准备

- **必须读取 `engine.md`**：理解创意引擎和当前章节在引擎中的位置
- **必须读取大纲文件**：正文生成基于大纲的情节点
- **必须读取世界观知识库**：确保描写符合原作设定
- **推荐读取 `style_notes.md`**：如果有案例参考，对标文笔水平
- **续写时必须读取最近2-3章正文**：保持风格连贯
- **参考文档**：本Skill包含完整的参考文档，Agent应直接读取使用
- **输出格式**：Markdown格式的正文文件

---

## ⭐ 大纲到正文转化指南（核心）

这是本Skill的核心能力：将大纲的**信息**转化为正文的**情感体验**。

### 转化核心原则

```
大纲告诉读者：会发生什么（信息）
正文让读者：感受到什么（体验）

大纲："索隆面临抉择，最终选择帮助李寒"
正文：让读者心跳加速、为索隆的选择而感动
```

### 1. 情节点 → 场景

**转化规则**：1个情节点 = 1-2个场景

**大纲情节点**：
```
【围攻】老鼠带人包围广场。李寒寡不敌众，被逼入绝境。索隆拿起刀，却转身朝出口走去。
```

**正文场景化**：
```
场景A（500字）：包围
- 时间/地点：黄昏，广场
- 视角：李寒
- 感官重点：脚步声（听觉）、汗水（触觉）、血腥味（嗅觉）
- 情绪目标：绝望、被困

场景B（400字）：索隆离去
- 视角：索隆背影 → 李寒的注视
- 感官重点：脚步声渐远（听觉）
- 情绪目标：被抛弃的绝望
```

### 2. 核心张力 → 情感体验

**关键**：不是告诉读者"他在抉择"，而是让读者**感受到**抉择的痛苦

**大纲核心张力**：
```
索隆必须选择——帮助这个陌生的海军，还是趁乱离开追寻自己的梦想？
```

**错误的正文（告诉）**：
```
索隆陷入了两难的抉择。一边是自己的梦想，一边是这个陌生人的求助。他不知道该怎么选择。
```

**正确的正文（展现）**：
```
索隆的脚步停了。

不是因为身后那个海军的惨叫——他听过太多这种声音。

是因为他的脚不听使唤。

"切。"他低声骂了一句，也不知道是在骂谁。

世界第一剑豪。这是他对古伊娜的承诺。每一天，每一刻，他都在为这个目标而活。

可是——

"真正的强者，从不逃避。"

耕四郎的声音突然在脑海中响起，清晰得像是就在耳边。

索隆的手缓缓握紧了刀柄。
```

### 3. 情感曲线 → 节奏控制

**大纲情感曲线**：
```
低谷(被围困) → 更低(索隆离去) → 转折(索隆回头) → 上升(并肩作战)
```

**转化为节奏控制**：

| 情感阶段 | 节奏 | 字数占比 | 描写策略 |
|---------|------|---------|---------|
| 低谷(被围困) | 慢 | 20% | 感官细节丰富、时间拉长、环境压迫 |
| 更低(索隆离去) | 更慢 | 15% | 内心独白、动作放慢、每一步都写 |
| 转折(索隆回头) | 突然加速 | 10% | 短句、动作词、"砰""锵"等拟声词 |
| 上升(并肩作战) | 快 | 55% | 动作密集、对话简短、一气呵成 |

**低谷时的慢节奏示例**：
```
汗水从李寒的额头滚落，滑过眼角，带着一丝刺痛。

他的双臂开始发抖。不是因为恐惧——是因为已经连续格挡了三十七次。

三十七次。他在心里默数。

第三十八次攻击来了。

他挡住了。但膝盖撞到了地面。

（时间被拉长，每个动作都被放大）
```

**转折时的快节奏示例**：
```
脚步声停了。

刀出鞘。

索隆转身。

"让开。"

两个字，却比任何誓言都要沉重。

（短句、快节奏、冲击力）
```

### 4. 三层冲突 → 潜台词与行动

**大纲三层冲突**：
```
- 内在：索隆的梦想 vs 内心的正义感
- 人际：李寒与索隆的信任建立
- 外在：老鼠势力的围攻
```

**转化策略**：

| 冲突类型 | 正文表现方式 | 示例 |
|---------|-------------|------|
| **内在冲突** | 内心独白、矛盾行为 | 索隆的脚步停了——他的身体比脑子更快做出了选择 |
| **人际冲突** | 对话潜台词、微表情 | "我不是在帮你。"（嘴上说不是，但刀已经出鞘） |
| **外在冲突** | 环境压迫、具体威胁 | 包围圈越收越紧，火把的光芒在刀刃上跳动 |

**人际冲突的潜台词示例**：

```
"你为什么帮我？"李寒的声音沙哑。

索隆没有看他。

"我不是在帮你。"他的声音冷得像冰，"我只是看不惯这种以多欺少的卑鄙行径。"

李寒笑了。嘴角裂开的伤口渗出血来，但他还是笑了。

"是吗。"

他没有说"谢谢"。

索隆也没有期待他说。

（表面冷漠，实际已经产生了某种默契）
```

### 5. 角色弧光片段 → 关键Moment

**大纲角色弧光**：
```
索隆从"只顾自己的梦想"向"愿意为值得的人出手"迈出第一步
```

**转化为关键Moment**（必须用具体行动展现，不能用旁白说明）：

**错误示范（旁白说明）**：
```
在这一刻，索隆的内心发生了变化。他意识到，有些事情比梦想更重要。
```

**正确示范（行动展现）**：
```
老鼠的刀劈向李寒的后背。

索隆的身体动了。

比他的思考更快。

"铛——！"

三把刀同时格挡，火花四溅。

"你——"李寒惊讶地回头。

索隆没有说话。他也在惊讶。

惊讶于自己竟然会为一个认识不到一天的人挡刀。

（用行动展现内在变化，比任何旁白都有力）
```

### 6. 章节节奏类型 → 整体字数分配

根据大纲的**章节节奏标记**，决定整章的写法：

| 章节类型 | 总字数 | 场景密度 | 描写风格 |
|---------|-------|---------|---------|
| **蓄势章** | 4000-5000 | 低（3-4个场景） | 细腻、铺垫、内心戏多 |
| **爆发章** | 5000-7000 | 高（6-8个场景） | 紧凑、动作密集、对话快 |
| **释放章** | 4000-6000 | 中（4-5个场景） | 情感宣泄、收尾、余韵 |
| **过渡章** | 3000-4000 | 低（2-3个场景） | 轻松、日常、伏笔埋设 |

---

## 操作步骤

### 标准流程（强制渐进式交互）

**⚠️ 重要执行顺序：**
1. 第1步：加载大纲与世界观
2. 第2步：解析章节结构
3. 第3步：场景规划
4. 第4步：逐场景生成（每个场景等待用户确认）
5. 第5步：质量检查
6. 第6步：拼接输出

#### 第1步：加载引擎、大纲与世界观（必须）

**必须读取以下文件**：

1. **引擎文件**（必须，最重要）：
   ```
   read_file engine.md
   ```
   - 理解当前章节在引擎中的位置
   - 确认引擎状态（启动/加速/反噬/升级）
   - 了解本章冲突如何源于引擎

2. **大纲文件**：从用户指定路径或 `outline.md` 读取
   ```
   read_file [大纲文件路径]
   ```

3. **世界观知识库**（根据同人类型）：
   - 海贼王同人：`novel-outline-generator/references/onepiece_knowledge_base.md`
   - 火影同人：`novel-outline-generator/references/naruto-world.md`
   - 死神同人：`novel-outline-generator/references/bleach-world.md`

4. **角色声音指南**（必须）：
   ```
   read_file references/character_voice.md
   ```

5. **风格参考**（如果有，推荐）：
   ```
   read_file style_notes.md
   ```
   - 对标案例的文笔水平
   - 读取案例对应场景的参考段落

6. **续写时额外读取**：
   ```
   read_file [最近2-3章正文]    # 保持风格连贯
   read_file progress.md         # 恢复用户反馈和调整方向
   ```

**加载目的**：
- **确保正文展现引擎意识**——读者能感受到引擎的存在
- 确保正文描写符合原作设定
- 确保角色对话符合其性格和声音特点
- 确保场景描写符合世界观
- 续写时保持风格连贯

#### 第2步：解析章节结构（基于新大纲模板）

从大纲中提取目标章节的**完整信息**：

1. **章节概述**：了解本章核心内容
2. **章节节奏**：蓄势章/爆发章/释放章/过渡章 → 决定整体写法
3. **核心张力**：主角面临什么困境或抉择？ → 这是正文的**灵魂**
4. **情节点列表**：明确需要展开的情节点（通常5-7个）
5. **情感曲线**：低谷→上升→高峰等 → 决定节奏控制
6. **三层冲突**：内在/人际/外在 → 决定描写侧重
7. **角色弧光片段**：本章角色有什么内在变化？ → 需要关键Moment展现
8. **伏笔要求**：需要自然植入的伏笔

**输出格式**：
```
章节解析结果：
- 章节标题：《索隆的抉择》
- 引擎状态：加速阶段 —— 追杀迫近，主角被迫暴露能力
- 引擎在本章的体现：[具体描述冲突如何源于引擎]
- 章节节奏：爆发章 → 紧凑写法，动作密集
- 核心张力：索隆必须选择帮助李寒还是离开
- 情节点数量：7个 → 预估7-10个场景
- 情感曲线：低谷 → 更低 → 转折 → 上升
- 三层冲突：
  · 内在：索隆的梦想 vs 正义感 → 内心独白重点
  · 人际：李寒与索隆的信任 → 对话潜台词重点
  · 外在：老鼠的围攻 → 环境压迫重点
- 角色弧光：索隆迈出"为他人出手"的第一步 → 需要关键Moment
- 伏笔：索隆的"约定"
- 案例对标：[如果有style_notes.md，标注参考哪本书的哪个场景]
```

#### 第3步：场景规划（应用转化指南）

将情节点转化为可写的场景，**同时规划情感目标**：

**场景划分原则**：
- 1个情节点 = 1-2个场景
- 每个场景有独立的：时间、地点、参与角色、**情感目标**

**场景字数自适应**（根据情感曲线位置）：
| 情感位置 | 字数范围 | 节奏 | 描写策略 |
|---------|---------|------|---------|
| 低谷 | 500-800字 | 慢 | 感官细节丰富、时间拉长 |
| 转折 | 300-500字 | 突快 | 短句、动作词、冲击力 |
| 上升/高峰 | 800-1500字 | 快 | 动作密集、对话简短 |
| 释放/余韵 | 400-600字 | 慢 | 情感沉淀、留白 |

**场景规划模板（新）**：
```
场景规划：
1. 场景1：[场景名称]
   - 情感位置：[低谷/上升/转折/高峰/释放]
   - 情感目标：读者应该感受到什么？
   - 预估字数：XXX字
   - 参与角色：[角色列表]
   - 核心内容：[一句话概括]

2. 场景2：...
```

**必须展示场景规划给用户确认，等待用户确认后才能开始生成。**

#### 第4步：逐场景生成（核心步骤，应用转化指南）

**⚠️ 这是正文生成的核心步骤，必须严格遵循**

**生成顺序**：
1. 生成第1个场景
2. **展示给用户，等待确认**
3. 用户确认后，生成第2个场景
4. **展示给用户，等待确认**
5. 重复直到所有场景完成

**前3章特殊处理**：
- 第1章：必须生成3个版本，用户选最好的，反馈后改进
- 第2-3章：推荐生成2个版本
- 如果有案例对标（`style_notes.md`），每个版本都要对标案例水平

**每个场景生成时，必须应用**：

1. **引擎意识**（最重要的新增）
   - 确认本场景的冲突是否源于引擎
   - 根据引擎阶段选择描写氛围（启动=不安、加速=压迫、反噬=绝望、升级=震撼）
   - 让读者在字里行间感受到引擎的存在

2. **大纲转化原则**（见上方"大纲到正文转化指南"）
   - 核心张力 → 用场景展现，不是告诉
   - 情感曲线 → 控制节奏（低谷慢、转折快）
   - 三层冲突 → 内在用独白、人际用潜台词、外在用环境
   - 角色弧光 → 用行动展现，不是旁白

3. **案例对标**（如果有 `style_notes.md`）
   - 关键场景（开场、高潮、转折）必须对标案例
   - 对标文笔水平、情感力度、角色味道

4. **描写金字塔**（见 `references/writing_techniques.md`）
   - 底层：基础叙述（发生了什么）
   - 中层：感官细节（视觉、听觉、触觉、嗅觉、味觉）
   - 顶层：情感共鸣（让读者感同身受）

3. **节奏控制**（根据场景的情感位置）
   - 低谷场景：慢节奏，每个动作都写，时间拉长
   - 转折场景：突然加速，短句，冲击力
   - 高峰场景：快节奏，动作密集，一气呵成
   - 释放场景：慢下来，情感沉淀，留白

4. **角色声音系统**（见 `references/character_voice.md`）
   - 对话必须符合角色的说话特点
   - 内心独白必须符合角色的思维方式

5. **OOC检测**
   - 检查对话是否符合角色性格
   - 检查行为是否符合角色逻辑

**关键禁止项**：
- ❌ **禁止**：一次性生成整章
- ❌ **禁止**：跳过用户确认
- ❌ **禁止**：连续3句以上纯叙述（流水账警告）
- ❌ **禁止**：对话无神态动作
- ❌ **禁止**：用旁白说明角色心理变化（要用行动展现）
- ❌ **禁止**：告诉读者"他很紧张"（要让读者感受到紧张）

#### 第5步：质量检查

每个场景生成后，自动进行质量检查：

**检查项**：
1. **流水账检测**
   - 连续3句以上纯叙述 → 警告
   - 没有感官描写 → 警告
   - 对话无神态动作 → 警告

2. **OOC检测**
   - 对话是否符合角色性格
   - 行为是否符合角色逻辑

3. **节奏检测**
   - 场景节奏是否合适
   - 情感张力是否到位

**如果检测到问题，必须修正后再展示给用户。**

#### 第6步：拼接输出

所有场景确认后：
1. 按顺序拼接所有场景
2. 检查场景衔接是否流畅
3. 添加章节标题和分隔符
4. 输出完整章节正文

**输出格式**：
```markdown
## 第X章：《章节标题》

[场景1正文]

[场景2正文]

...

[场景N正文]
```

---

## 文笔提升机制

### 描写金字塔

```
顶层: 情感共鸣（让读者感同身受）
      ↑
中层: 感官细节（视觉、听觉、触觉、嗅觉、味觉）
      ↑
底层: 基础叙述（发生了什么）

优质正文 = 三层交织 | 流水账 = 只有底层
```

**应用示例**：
- ❌ 流水账："李寒走进了房间，看到了索隆，索隆正在练剑。"
- ✅ 优质正文："李寒推开斑驳的木门，一股汗水和铁锈混合的气息扑面而来。昏暗的练武场中央，索隆赤裸着上身，三把刀在空气中划出银色的弧线，嗡嗡的破空声震得李寒耳膜发麻。汗珠从索隆青筋暴起的手臂上滚落，在地面砸出深色的印记。李寒心中一凛——这家伙，比情报中描述的还要疯狂。"

### 避免流水账检测

以下情况会触发警告：
1. **连续3句以上纯叙述** → 必须加入感官描写或情感
2. **没有感官描写** → 至少使用2种感官（视觉、听觉、触觉、嗅觉、味觉）
3. **对话无神态动作** → 对话必须配合动作、表情、语气

---

## 人物刻画机制

### 角色声音系统

为每个主要角色定义独特的"声音"：

| 角色 | 说话特点 | 内心独白风格 | 典型用词 |
|------|---------|-------------|---------|
| 索隆 | 冷淡、自信、话少 | 战斗思维、目标导向 | "切"、"有点意思" |
| 路飞 | 直接、单纯、大嗓门 | 简单直接、以吃为主 | "哈哈哈"、"真好吃" |
| 娜美 | 精明、泼辣、理性 | 金钱敏感、计算得失 | "钱"、"白痴" |
| 山治 | 绅士（对女）、毒舌（对男） | 美食思维、骑士精神 | "小姐"、"臭剑士" |

详见 `references/character_voice.md`

### OOC检测

**检查维度**：
1. **对话检测**：这句话是这个角色会说的吗？
2. **行为检测**：这个行为符合角色性格吗？
3. **反应检测**：面对这个情况，角色会这样反应吗？

**OOC示例**：
- ❌ OOC：索隆说"哎呀，好害怕啊"（不符合性格）
- ✅ IC：索隆冷笑一声："就这点实力？"

---

## 爽点放大机制

### 爽点节奏公式

```
抑(30%) → 扬(70%)

铺垫部分(30%): 困境、压力、期待
释放部分(70%): 转折(10%) + 爽感(40%) + 收尾(20%)
```

### 四种爽点放大技巧

详见 `references/climax_design.md`

#### 1. 能力提升爽

**公式**：强调之前弱小 → 突破有仪式感 → 展示新能力

**示例**：
```
【铺垫】李寒挣扎着站起来，浑身是血，对面的敌人狞笑着走来。
【转折】丹田中的能量突然沸腾，一股陌生而强大的力量涌遍全身。
【爽感】黑色的霸气覆盖了李寒的双臂，空气仿佛都在颤抖。敌人脸上的狞笑凝固了。
【收尾】"武装色...霸气？"敌人的声音开始颤抖。
```

#### 2. 打脸爽

**公式**：对方嘲讽具体 → 主角隐忍 → 反转有力度 → 嘲讽者崩溃

**示例**：
```
【嘲讽】"就凭你一个三等兵？也配和我说话？"老鼠上校不屑地看着李寒。
【隐忍】李寒低下头，嘴角却勾起一丝笑意。
【反转】"上校大人，这是准将大人的密令。"李寒掏出一封信，"任命我...接管您的部队。"
【崩溃】老鼠的脸瞬间白了。
```

#### 3. 智力碾压爽

**公式**：展示复杂局面 → 众人束手无策 → 揭示主角布局 → 恍然大悟

#### 4. 势力扩张爽

**公式**：展示收服难度 → 逐步攻克 → 收服过程有逻辑 → 势力壮大

---

## 续写恢复机制（新增）

当用户退出后回来续写正文时，必须执行以下恢复流程：

1. **读取 `engine.md`** → 恢复引擎设计，理解当前引擎状态
2. **读取 `outline.md`** → 恢复弧线规划和章节大纲
3. **读取最近2-3章正文** → 恢复叙事节奏、文风、悬念
4. **读取 `progress.md`** → 恢复用户反馈和风格调整方向
5. **读取 `style_notes.md`**（如果有）→ 恢复风格基准和案例参考

恢复后向用户确认：
- 上次写到哪里（第几章第几个场景）
- 最后的文风和节奏状态
- 上次的用户反馈和调整
- 接下来要写的内容

**保持连贯的关键**：
- 分析最近2-3章的词汇选择、句式长度、节奏快慢
- 确保续写的第一段能自然衔接前文
- 不要突然改变文风（除非用户要求）

---

## 资源索引

### 引擎与风格文档（必须读取）
- **项目引擎文件**：`engine.md`
  - 用途：理解创意引擎和当前章节在引擎中的位置
  - 何时读取：**每次生成正文前必须读取**
- **世界观语言风格指南**：`novel-outline-generator/references/world_language_styles.md`
  - 用途：确保语言风格匹配世界观，穿越者语言适配
  - 何时读取：**首次生成正文时必须读取**
- **风格参考**：`style_notes.md`（如果有）
  - 用途：案例对标，确保文笔水平达标
  - 何时读取：写关键段落时读取

### 核心参考文档（必须读取）

- **写作技法指南**：见 [references/writing_techniques.md](references/writing_techniques.md)
  - 用途：描写金字塔、感官描写技巧、情感渲染方法
  - 何时读取：**每次生成正文前必须读取**

- **角色声音指南**：见 [references/character_voice.md](references/character_voice.md)
  - 用途：角色对话风格、内心独白风格、典型用词
  - 何时读取：**每次生成正文前必须读取**

- **描写模式库**：见 [references/description_patterns.md](references/description_patterns.md)
  - 用途：环境描写、动作描写、心理描写的模式库
  - 何时读取：需要描写灵感时读取

### 辅助参考文档

- **对话写作指南**：见 [references/dialogue_guide.md](references/dialogue_guide.md)
  - 用途：对话写作技巧、潜台词、对话节奏
  - 何时读取：写对话场景时读取

- **节奏控制指南**：见 [references/pacing_rhythm.md](references/pacing_rhythm.md)
  - 用途：场景节奏、章节节奏、情感张力
  - 何时读取：需要控制节奏时读取

- **爽点放大技巧**：见 [references/climax_design.md](references/climax_design.md)
  - 用途：四种爽点类型、放大技巧、节奏公式
  - 何时读取：写爽点场景时读取

### 可复用大纲Skill的文档

- **世界观知识库**：`novel-outline-generator/references/onepiece_knowledge_base.md` 等
- **角色塑造指南**：`novel-outline-generator/references/character_guide.md`

### 辅助脚本

- **正文质量检查器**：见 [scripts/text_quality_checker.py](scripts/text_quality_checker.py)
  - 功能：流水账检测、OOC检测、节奏检测
  - 调用示例：
    ```bash
    python scripts/text_quality_checker.py --input chapter.md
    ```

### 示例文档

- **优秀正文示例**：见 [assets/excellent_examples.md](assets/excellent_examples.md)
- **常见错误案例**：见 [assets/common_mistakes.md](assets/common_mistakes.md)

---

## 注意事项

### 核心原则

1. **引擎意识（最重要的新增）**：正文必须展现引擎的存在
   - 读取 `engine.md`，理解当前章节在引擎中的位置
   - 每个场景的冲突应该能回溯到引擎
   - 让读者在字里行间感受到那个不可逃避的核心矛盾
2. **大纲转化**：正文的任务是把大纲的**信息**转化为读者的**情感体验**
   - 大纲说"索隆抉择" → 正文让读者**感受到**抉择的痛苦
   - 大纲说"角色成长" → 正文用**行动**展现，不是旁白说明
3. **前3章多版本（强制）**：前3章必须生成多版本供用户选择
4. **案例对标**：关键段落对标案例水平（如果有 `style_notes.md`）
5. **渐进式生成（强制）**：每个场景生成后必须等待用户确认，禁止一次性生成整章
6. **节奏控制（强制）**：根据大纲的情感曲线控制快慢
   - 低谷 = 慢节奏（时间拉长、感官细节丰富）
   - 转折 = 突然加速（短句、动作词）
   - 高峰 = 快节奏（动作密集、对话简短）
4. **展现而非告诉**：
   - ❌ "他很紧张" → ✅ "汗水顺着脊背滑落，握刀的手微微发抖"
   - ❌ "她变了" → ✅ 用具体行动展现变化
5. **文笔优先**：正文的核心是文笔质量，不是剧情推进速度
6. **角色声音**：每个角色的对话必须符合其独特的"声音"
7. **避免流水账**：三层描写金字塔，禁止连续纯叙述

### 工具系统

写正文时使用以下工具管理创作状态：

#### 写之前（必须执行）

```python
# 0. 读取引擎文件（最重要！）
read_file("engine.md")            # 理解引擎状态和当前章节位置

# 1. 查看进度和状态
get_progress()                    # 查看当前进度，确定要写哪章
list_characters()                 # 查看所有角色状态
list_foreshadows()                # 查看待回收伏笔，寻找回收机会

# 2. 获取具体信息
get_character("索隆")             # 获取角色详细档案
recall("summary", "第N-1章")      # 回忆上一章内容，保持连贯
recall("decision")                # 查看用户之前的决策

# 3. 续写时额外读取
read_file("最近2-3章正文")         # 保持文风连贯
read_file("progress.md")          # 恢复用户反馈
read_file("style_notes.md")       # 恢复风格基准（如果有）
```

#### 写之后（推荐使用专用工具）

```python
# 方式1：使用章节完成工具（推荐，自动保存+更新进度+记录摘要）
complete_chapter(5, 正文内容, "本章摘要：李寒收服索隆...", "收服索隆")

# 方式2：手动更新（如果需要更细粒度控制）
update_character("索隆", status="已收服", note="第5章收服，原因是...")
add_relationship("李寒", "索隆", "上下级", mutual=True)
resolve_foreshadow("老鼠交易", resolved_chapter=5, resolution="揭示老鼠被捕")
update_progress(writing_completed=5, current_chapter=6)
```

#### 工具速查表

| 工具 | 用途 | 时机 |
|-----|------|------|
| `get_progress()` | 查看进度 | 写前 |
| `list_characters()` | 角色状态概览 | 写前 |
| `get_character(name)` | 角色详情 | 写前 |
| `list_foreshadows()` | 待回收伏笔 | 写前 |
| `complete_chapter(ch, content, summary, title?)` | 完成正文 | 写后 |
| `update_character(name, status?, location?, note?)` | 更新角色 | 角色变化时 |
| `resolve_foreshadow(name, chapter?, resolution?)` | 回收伏笔 | 伏笔回收时 |
| `plant_foreshadow(name, content, chapter?, target?)` | 埋设伏笔 | 需要时 |

### 与大纲的关系

- **大纲是骨架，正文是血肉**
- 正文必须基于大纲的情节点，但可以在细节上发挥
- 大纲的情节点是"写什么"，正文要解决"怎么写"
- 如果大纲情节点有问题，应建议用户先修改大纲

### 质量检查

- **流水账检测**：连续3句纯叙述 → 警告
- **感官缺失**：没有感官描写 → 警告
- **对话呆板**：对话无神态动作 → 警告
- **OOC检测**：对话/行为不符合角色 → 警告

### 字数控制

- **场景字数自适应**：根据情节复杂度调整
- **章节总字数**：通常 3000-8000 字
- **不追求字数**：质量优先于数量

---

## 使用示例

### 示例1：根据大纲写正文

**用户**：根据大纲写第3章正文

**Agent 工作流程**：

1. **读取大纲和世界观**
   ```
   read_file [大纲路径]
   read_file references/character_voice.md
   read_file references/writing_techniques.md
   ```

2. **解析第3章结构**
   ```
   章节解析结果：
   - 章节标题：《意外的收获》
   - 情节点数量：5个
   - 预估场景数：8个
   - 爽点位置：第4、5情节点
   - 情感轨迹：紧张 → 意外 → 兴奋
   ```

3. **场景规划（展示给用户确认）**
   ```
   场景规划：
   1. 场景1：老鼠事件发现
      - 类型：普通
      - 预估字数：600字
      - 参与角色：李寒、老鼠手下
      - 核心内容：李寒发现老鼠偷运物资

   2. 场景2：暗中收集证据
      - 类型：简单
      - 预估字数：400字
      ...
   ```
   **等待用户确认**

4. **逐场景生成**

   **场景1：老鼠事件发现**
   ```
   夜幕低垂，谢尔兹镇的军需仓库外一片寂静。李寒靠在墙角的阴影里，目光锁定着远处鬼鬼祟祟的几个身影。

   "嘿，小心点！这批货要是出了差错，老鼠大人会扒了咱们的皮！"

   李寒眯起眼睛。老鼠...果然是那个上校在搞鬼。他的嘴角勾起一丝冷笑，心中却在飞速盘算——这可是送上门的机会。

   汗水顺着脊背滑落，夜风带着海水的咸腥味吹过，李寒屏住呼吸，将眼前的一切默默记在心里。
   ```
   **等待用户确认**

   **场景2：暗中收集证据**
   ...

5. **质量检查**
   - 流水账检测：通过
   - 感官描写：通过（视觉、听觉、触觉、嗅觉）
   - OOC检测：通过

6. **拼接输出**
   ```
   write_file 第3章_意外的收获.md
   ```

### 示例2：优化正文描写

**用户**：让这段战斗场景更激烈

**Agent 工作流程**：

1. **读取原文**
   ```
   read_file [正文路径]
   ```

2. **分析问题**
   - 缺少感官细节
   - 动作描写单调
   - 情感张力不足

3. **展示优化对比**

   **原文**：
   ```
   索隆挥刀砍向李寒，李寒躲开了。两人又交换了几招，索隆落入下风。
   ```

   **优化后**：
   ```
   索隆暴喝一声，三把刀同时劈落，带起尖锐的破空声！

   李寒侧身闪避，刀锋擦着他的衣袖划过，撕裂的布料在空中飘舞。他的瞳孔微微收缩——这家伙的力量，比想象中还要恐怖。

   "不错。"李寒吐出两个字，手腕一翻，反手一刀直取索隆咽喉。

   索隆眼中闪过一丝惊讶，他的身体向后仰去，刀锋从他鼻尖前不到一寸的地方掠过。汗珠从他额头滚落，落在地面上发出轻微的"啪嗒"声。

   "你..."索隆的声音有些沙哑，"比我想象的强。"
   ```

   **等待用户确认**

4. **用户确认后更新文件**

### 示例3：续写正文（断点恢复机制）

**用户**：续写第5章

**Agent 工作流程**：

1. **状态恢复（关键步骤）**

   ```python
   # 1.1 查看当前进度
   get_progress()
   # 返回：大纲 10/50，正文 4/50，当前章节 5

   # 1.2 读取已有正文（如果存在）
   read_file chapters/volume-1/chapter-5.md
   # 分析断点位置：已完成场景1-3，场景4写了一半

   # 1.3 恢复上下文
   list_characters()              # 恢复角色状态
   list_foreshadows()             # 恢复伏笔状态
   recall("summary", "第4章")     # 恢复前文记忆
   ```

2. **断点分析（展示给用户）**

   ```
   【续写状态分析】

   📍 断点位置：第5章，场景4中途

   ✅ 已完成场景：
   - 场景1：李寒抵达谢尔兹镇（600字）
   - 场景2：遇见被绑的索隆（500字）
   - 场景3：与老鼠交涉（800字）

   📝 未完成场景：
   - 场景4：释放索隆（已写300字，预计还需500字）
   - 场景5：击败老鼠（未开始）
   - 场景6：索隆的决定（未开始）

   🎯 断点处最后一段：
   > "索隆的眼神中闪过一丝惊讶，他没有想到这个海军会..."

   请确认从这里续写，还是需要调整？
   ```

3. **续写执行**

   ```python
   # 从断点处继续，保持文风一致
   # 场景4（续写部分）：
   """
   ...会选择站在自己这边。

   "你为什么帮我？"索隆的声音沙哑，带着几天未进食的虚弱。

   李寒蹲下身，与他平视："因为你有我需要的东西。"

   "什么？"

   "剑术。"李寒的嘴角勾起一丝笑意，"还有...一颗不愿屈服的心。"
   """
   ```
   **展示给用户确认**

4. **逐场景继续**
   - 完成场景4 → 等待确认
   - 生成场景5 → 等待确认
   - 生成场景6 → 等待确认

5. **完成保存**

   ```python
   complete_chapter(5, 完整正文, "本章摘要：李寒释放索隆，击败老鼠，索隆决定跟随", "谢尔兹镇之战")
   ```

**续写注意事项**：
- ⚠️ 必须读取已有正文，分析断点位置
- ⚠️ 保持文风一致（词汇、句式、节奏）
- ⚠️ 不要重复已写内容
- ⚠️ 场景衔接要自然

### 示例4：修改正文（问题诊断机制）

**用户**：这段战斗描写太平淡了，帮我优化

**Agent 工作流程**：

1. **问题诊断（系统化分析）**

   ```
   【问题诊断报告】

   📄 分析范围：第5章，场景5（击败老鼠）

   🔍 检测结果：

   ┌─────────────────────────────────────────┐
   │ 问题类型          │ 严重程度 │ 位置       │
   ├─────────────────────────────────────────┤
   │ 流水账            │ ⚠️ 中    │ 第3-6行    │
   │ 感官缺失          │ ⚠️ 中    │ 全段       │
   │ 动作单调          │ ❌ 高    │ 第7-12行   │
   │ 情感张力不足      │ ❌ 高    │ 高潮部分   │
   │ 对话呆板          │ ⚠️ 中    │ 第15行     │
   └─────────────────────────────────────────┘

   📊 质量评分：⭐⭐☆☆☆ (2/5)

   💡 根本原因分析：
   1. 缺乏描写金字塔的中层（感官细节）和顶层（情感共鸣）
   2. 战斗动作仅用"打"、"躲"等单一动词
   3. 爽点没有铺垫，直接进入结果
   ```

2. **优化方案展示（对比机制）**

   ```
   【优化对比】

   ════════════════════════════════════════
   ❌ 原文（问题标注版）
   ════════════════════════════════════════

   老鼠大喊："你敢反抗我？"          ← 对话呆板，缺乏情绪
   李寒冲上去打他。                   ← 流水账，动作单调
   老鼠躲开了。                       ← 流水账
   李寒又打了一拳。                   ← 动作单调，重复
   这次打中了。                       ← 流水账
   老鼠倒在地上。                     ← 缺乏爽感释放

   ════════════════════════════════════════
   ✅ 优化后（应用描写金字塔）
   ════════════════════════════════════════

   "你敢——"老鼠的怒吼戛然而止。

   因为李寒的拳头，已经抵在了他的鼻尖前。

   空气仿佛凝固。老鼠瞪大了眼睛，瞳孔中倒映着李寒冰冷的面庞。
   那只拳头稳如磐石，没有丝毫颤抖。

   "你...你什么时候..."老鼠的声音开始发颤。

   李寒没有回答。他的手腕轻轻一转——

   "砰！"

   拳风呼啸而出，老鼠的身体像断了线的风筝一样飞出去，
   重重地撞在墙上，砖石崩裂，尘土飞扬。

   "咳...咳咳..."老鼠趴在废墟中，试图爬起来，
   却发现自己的腿已经不听使唤。

   他抬起头，看见李寒正慢慢走来。
   那双眼睛里没有愤怒，没有得意，只有...平静。

   仿佛刚才碾压的，不过是一只蝼蚁。

   ════════════════════════════════════════
   📊 优化效果对比
   ════════════════════════════════════════

   │ 指标        │ 原文  │ 优化后 │ 提升  │
   ├─────────────┼───────┼────────┼───────┤
   │ 字数        │ 45    │ 280    │ +522% │
   │ 感官描写    │ 0种   │ 3种    │ +3    │
   │ 动作多样性  │ 2个   │ 8个    │ +300% │
   │ 情感张力    │ ⭐    │ ⭐⭐⭐⭐│ +3    │
   │ 整体评分    │ 2/5   │ 4/5    │ +100% │
   ```

3. **用户选择**

   ```
   请选择：
   A. 采用优化版本（推荐）
   B. 在原文基础上微调（保留原文风格）
   C. 提供更多优化方向，我来选择
   D. 只优化特定问题（如：只改动作描写）
   ```

4. **确认后应用修改**

**修改注意事项**：
- ⚠️ 必须展示问题诊断报告
- ⚠️ 必须提供对比（原文 vs 优化后）
- ⚠️ 必须量化优化效果
- ⚠️ 给用户选择权，不强制应用

---

## 常见问题

### Q1：正文和大纲不一致怎么办？

**A**：正文必须基于大纲的情节点，但可以在以下方面发挥：
- 具体的对话内容
- 感官描写细节
- 过渡场景
- 角色的微表情和小动作

如果发现大纲情节点有逻辑问题，应建议用户先使用大纲Skill修改大纲。

### Q2：如何控制字数？

**A**：字数自适应规则：
- 简单场景：300-600字（过渡、日常）
- 普通场景：600-1000字（对话、叙述）
- 复杂场景：1000-2000字（战斗、高潮）

不要为了凑字数而水字数，质量优先。

### Q3：如何避免流水账？

**A**：使用描写金字塔：
- 确保不连续超过3句纯叙述
- 每个场景至少使用2种感官描写
- 对话必须配合动作、表情、语气
- 适时加入角色的内心独白

### Q4：如何处理爽点场景？

**A**：使用爽点节奏公式：
- 铺垫(30%)：困境、压力、期待
- 转折(10%)：关键突破点
- 爽感(40%)：能力展示、打脸、碾压
- 收尾(20%)：反应、回味、钩子

详见 `references/climax_design.md`
